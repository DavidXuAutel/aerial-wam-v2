# E1r2 Handoff — 2026-09-03 夜

接上一轮工作，直接继续下一步。

## 当前状态

Branch: `feat/phase2-goal-scene-nav`  
最新 commit: `f26f8de` (fix phase2: fix 3 critical scene-intent defects)

### 本轮做了什么

1. **跑了 E1 forensics**（200 步，`--traj-out`），发现三个致命缺陷
2. **修了三个缺陷**（已提交推送）：
   - `_should_replan`：去掉 `d_fwd<d_clear` 每步触发（原来森林里 100% 触发，hold 机制完全失效）
   - `_score w_fwd=2.0`（原 0.5）：软惩罚 max 50m > toward_g 进展 25m，扇面现在真正起作用
   - `wam_phase2_long_eval`：加 F-tele 瞬移检测（>20m 跳变标记 episode 失效）
3. **Forensics 发现 Route 1 在 step 72 发生 580m 瞬移**（AirSim 110 环境 bug），原 E1 route 1 数据（在 125 跑）无此问题

## 下一步：跑 E1r2（验证修复后行为）

### 在 125 上跑 route 1（原始路由）

```bash
cd ~/aerial-wam-v2
git pull  # 拉 f26f8de
/home/yao/sim_verify/.venv/bin/python \
  -m experiments.aerial.scripts.wam_phase2_long_eval \
  --subgoal-source scene --planner \
  --routes 1 --max-steps 400 \
  --traj-out artifacts/traj_e1r2_125 \
  --out artifacts/wam_phase2_e1r2_scene_r02_125.json \
  2>&1 | tee logs/e1r2_scene_r02_125_$(date +%Y%m%d_%H%M%S).log
```

### 在 110 上跑 route 0

```bash
cd ~/aerial-wam-v2
git pull  # 拉 f26f8de
/home/a26125/sim_verify/.venv/bin/python \
  -m experiments.aerial.scripts.wam_phase2_long_eval \
  --subgoal-source scene --planner \
  --routes 0 --max-steps 400 \
  --traj-out artifacts/traj_e1r2_110 \
  --out artifacts/wam_phase2_e1r2_scene_r01_110.json \
  2>&1 | tee logs/e1r2_scene_r01_110_$(date +%Y%m%d_%H%M%S).log
```

### 完成后合表（在 Mac 或任一台）

```bash
# 先把两台的 JSON 收到同一台
python -m experiments.aerial.scripts.merge_phase2_split_eval \
  --out artifacts/wam_phase2_e1r2_scene_merged.json \
  artifacts/wam_phase2_e1r2_scene_r01_110.json \
  artifacts/wam_phase2_e1r2_scene_r02_125.json
```

## 判定目标

E1 Gate（开跑前锁定，不得下调）：

| # | 判据 | 阈值 |
|---|------|------|
| G0 | 扇面有效 | `n_intent_offaxis > 0` 且 `max_intent_dev_deg ≥ 15°` |
| G1 | closure 不崩 | `mean_goal_closure ≥ 0.387` |
| G2 | 安全不回退 | `SCR ≤ 0.10` |
| G3 | 不散架 | `mean d_final ≤ 150m` |
| G4 | 干预不失控 | `IR ≤ 0.25` |
| G5 | 不宣称 200–500m | engineering probe only |

修复前 E1 结果（对照）：
- Route 0: offaxis=2/199(1%), d_final=143m, closure=0.07
- Route 1: **瞬移 invalidated**（step 72 跳 580m）

修复后预期（定性）：
- `_should_replan` 不再每步触发 → carrot 稳定持有 `replan_period=2s`
- `w_fwd=2.0` → d_fwd < ~15m 时开始真正偏转扇面
- offaxis 比率应明显高于 1%，且 dev_deg 应 > 15°（G0 可通过）

## 关键文件

```
experiments/aerial/rl/scene_intent.py          # SceneIntentPlanner（已修）
experiments/aerial/scripts/wam_phase2_long_eval.py  # eval（已修，含 F-tele）
experiments/aerial/rl/tests/test_scene_intent.py    # 15 个测试（全绿）
docs/handover/WAM_PHASE2_GOAL_SCENE_E1_DECLARE.md   # E1 活文档（需更新 E1r2 结果）
artifacts/wam_phase2_e1_scene_merged.json           # 原始 E1 结果（FAIL）
```

## 机器信息

- **110**: `a26125@10.229.20.110`（从 Mac: `ssh a26125-110`）
- **125**: `yao@10.229.20.125`（从 Mac: `ssh cursor-125`；125 本机直接跑）
- AirSim 只在 110 和 125 上各有一份，不要混跑

## 注意事项

1. Route 1 必须在 **125** 跑（110 的 AirSim 对 route 1 有瞬移 bug）
2. 两台 `--routes` 不得重叠（合表脚本会拒绝）
3. 活文档 `WAM_PHASE2_GOAL_SCENE_E1_DECLARE.md` 里的 §5/§6 只能填合表后的数字
4. E1r2 PASS → 才能把 CLI 默认改到 `scene`（独立 commit）
