# V4 Gate 状态活文档

> **用途**：V4-MVP merge 进度（镜像 V1 活文档）。  
> **设计**：[V4-MVP 规格](../superpowers/specs/2026-08-16-v4-mvp-design.md)（方案 1）。  
> **前置**：V1 merge PASS + `tau_predictor.kind=foe_calibrated`。

---

## 1. 一句话结论（2026-08-18）

**Goal+z0 track done — merge FAIL（① −8.74 / ④ PASS）** — ① **现规格下不可稳健达成**（不是差一点；措辞 2026-08-18 由「结构性不可达」收紧，见提案 §3 精确边界）。  
根因两叠加：(1) In 表规定 `act_latent(z)` / `V(z)`，goal 只进想象 reward；(2) 权威 300 iter 只有 `_mock_goal_episode()` 一对 start→goal。修好 conditioning（M5d）把**那一个**方向烙进 `π(z)`，① 从 −3.17 **退到** −8.74。逐 ep：首动作 cos≈−0.88 vs heur ≈+0.99。  
**未解释的部分（§A 已判定方向性）**：烙印解释首动作**恒定**。§A 三臂分解（2026-08-18）：常量前飞 λG0 **49.65** > 后退 **15.99**（A.3 同跑口径；首跑无 seed 为 47.02/15.9，已 superseded），差额来自 **progress**，p_coll≈0 → **不是**想象内碰撞项主导后退。见 [§A STATUS](V4_SIGNAL1_SA_DIAG_STATUS.md)。  
**⚠️ 「§4 充分」不成立；但「先修 RH」也无依据 —— A.3 判定作废（2026-08-18 复核）**：`b3_le_a` 字面成立（前飞@3.59 λG0 **59.85** ≤ π **103.63**），但臂无效 —— (1) 只匹配了 a0=3.59，π **轨迹**均值 ‖a‖ **≈16.5**（Σmaneuver 2.47÷0.01÷15 与 ‖goal_rel‖ 30→254 互证），欠匹配 ~4.6×；(2) `imagine()` **不夹动作**而部署夹 `body_delta_limits(0.2)`=[1.0,0.4,0.4,0.314]，五臂全在**不可实现区** —— **(b) `[+1,0,0,0]` 恰是可实现最大前飞，λG0 49.65 vs 可实现最大后退 15.99（3.1×）⇒ 可实现集合内 RH 方向偏好是对的**，π 只靠跑出集合 3.6–16× 才赢（`w_progress:w_maneuver=100:1` 下恒真）；(3) (b3)@3.59 第 ~8.4 步冲过 30 m 目标（末态 23.9 = |30−53.87| ✓），后半程 goal 在身后。`pi_imagined_arrival=false` 仍成立。见 [§A STATUS](V4_SIGNAL1_SA_DIAG_STATUS.md)。  
**§A.4 DONE（2026-08-18，seed=0）= `fwdmax_ge_pi`**：夹到 `body_delta_limits(1/5)`=[1.0,0.4,0.4,0.314] 后，最大前飞 λG0 **47.64 ≥** π_clipped **18.25**（π 夹后 a0 仍后向 `[-1.0, -0.309, -0.27, -0.163]`，goal 30→45.9）。倒挂来自**无界动作通道**，不是 RH 方向偏好。配套未夹 traj 匹配：`match_scale=15.59`（印证欠匹配 ~4.3–5.0×），(b3)@15.59 λG0 18.21、过冲 30→204；字面 `b3_le_a` **不**重开 RH 案。见 [§A STATUS](V4_SIGNAL1_SA_DIAG_STATUS.md) A.4 节。  
**✅ 动作空间一致性裁定 = C2（2026-08-18 签字，代码已落地 + 从零重训 + ① 再 gate）**：`n_action_clipped=0`（fresh 与训后 π 均是）；训后想象 π **前向** a0 x=+0.567，λG0 59.09 > 最大前飞 47.78。① 两跑均 **FAIL**（−7.43 / −3.53 vs heur ~9，**n=5 < 8** 非全权）；④ **PASS** 0.000 vs 0.400。判定 **`clip_insufficient`**。yaml 未翻。  
**⚠️ 但这是「方向已转正、① 仍 FAIL」，与事前判据措辞对不齐 —— 三个洞都在提出方（我），照 A.2/A.3/A.4 同例标注、不改写事前表**：  
1. **`clip_insufficient` 行的第二个合取项未验**。事前写的是「① 仍 FAIL **且** 首动作 cos 仍 <0」。① FAIL 已实测；但训后 §A 的 a0 x=**+0.567（前向）**，而 **①-eval 逐 ep 首动作 cos 没报**（M5d 报过 ≈−0.88）。π 仍 goal-blind ⇒ a0 只随 z 变、**不**随 goal 变，故 §A probe goal（`[+30,0,0.85]`，正前方）上的正 cos **不能**外推到评测 goal 分布。~~该数应已在 C2 re-gate 产物里，**须先取出**~~ ← **已测，见下段 cos diag DONE**。  
   > **更正（2026-08-18 同日，读码）**：上句「该数应已在 C2 re-gate 产物里」**是错的**。gate partial-1 只落 `actor_progress_sums` / `heuristic_progress_sums` / `actor_final_dists` / `heuristic_final_dists` / `scan` / `actor_ckpt` / `n_starts_scored`（`v4_gate_run_partials.py:228-234`），**没有** cos、没有动作；`run_progress_eval`（`v0_rollout_eval.py:584-634`）跑完两臂后直接**丢弃 episode**。出 cos 的是**另一个**已有工具 `experiments/aerial/scripts/v4_progress_diag.py`（M5d 跑过 ≈−0.88，**C2 未跑**）⇒ 必须**重跑一次 diag**（要 4090 渲染器），不是「取出已有的数」。  
2. **`clip_helped` 行的第一个合取项当初写错了**。「λG0(π_new) ≤ λG0(可实现最大前飞)」隐含「纯前飞是盒内最优」—— **四自由度盒里不成立**。实测 59.09 > 47.78 是**预期**，不是 exploit：`goal_rel` 30→17.8 即闭合 **12.2 m**，而 H=15、前向上限 1.0 m/步 ⇒ 盒内理论最大闭合 15 m，**12.2/15 = 81%** ⇒ 想象轨迹确在接近目标。**不**重开 RH 案（与提交方读法一致）。  
3. **n=5 不只是「不够 8」，更是非随机丢失**。spawn-in-collision 丢掉的是**杂乱起点**，存活 5 局偏向开阔空间 ⇒ 样本有**选择偏差**（① 为配对比较，配对内仍公平；但总体分布已偏）。**FAIL 方向对 n 稳健**：seed=0 要过需 mean ≥ 8.70×1.10 = 9.57 ⇒ 8 局和 ≥ 76.6，已有 5 局和 = −37.2 ⇒ 余 3 局须各 ≈ **37.9**（heuristic 自己每局仅 ~8.7，差 4.4×）；seed=1 同算须各 ≈ **33.7**。补满 n=8 翻不了 FAIL。**但两跑都不得入账为干净 gate**；同一治理规则下 **④ PASS 同为非全权**，且 off-rate 0.400 = **2/5**，比 ① 的 FAIL 更薄，且 on-rate=0 ⇒ ④b 大概率仍 `before_vacuous`（洞 2 已关的口径，④ 实证=④c）。  
   > **更正（2026-08-18，cos diag step 0 实测后）**：本条的「**选择偏差**」论据**撤回**。gate 两跑 `accepted` = **9 / 8**（扫描期被拒会**补扫**到数），掉到 scored=5 发生在**评测期**；而同 seed 同构造的 diag 拿到 **n=7 / n=8**（seed=1 **零丢局**）⇒ 丢局不是 spawn 扫描的性质，而在 **gate 的评测循环**（`_run_one_resilient→None`，或 ④ on/off 配对那条路径）。⇒ (i)「存活 5 局偏开阔空间」**无依据**，不得再引；(ii) **全权 ① n=8 很可能不必碰 spawn**，查 gate eval 循环即可，仍**不**降 `n`。附：`rejections["open_ahead"]` 两跑 **708 vs 15**（47×），扫描行为本身不稳，另记。  
4. **洞 4（2026-08-18 读码 + 实测确认）：评测 goal 不是分布，是构造性常量**。`make_obstacle_facing_episodes` 用 `goal_dist_m=30.0`（`v0_rollout_eval.py:251`），`heading=[cos yaw, sin yaw, 0]`、`goal = start + heading*30`、spawn `yaw` 与 heading **同一个**（`:386-389`）⇒ t=0 `goal_rel0` ≈ `[+30, 0, z]`。C2 diag 实测 |az| **≤ 0.8°**。后果：(i) 首动作 cos 退化为 `sign(a0_x)`，**不能**证「a0 随 goal 转」；(ii) In 表在 t=0 跨 ep 信息量为 0。另：`scan.rejections["spawn_collision"]` 是扫描期拒收；评测丢局是 `_run_one_resilient→None`（seed=0 accepted 9→scored 5）。键名 **`rejections`**。  
**✅ C2 ①-eval cos diag DONE（2026-08-18）**：125 `v4_progress_diag.py` 两 seed — mean cos(first_act, goal_body) actor **+0.806 / +0.762**（均 **≥ 0**，`n_cos<0=0`）。Pearson imagΣG vs real **+0.59 / +0.22** 但 imagΣG≈**+85** vs real progress≈**−5**。`cos_path_goal` mean 仅 **+0.08**（部分 ep 翻负）。事前表 ⇒ **不签** §4 In 表（活假设 = 想象-真实倒挂，非 goal-blind 固定负偏置）。详见 [`V4_C2_COS_DIAG_125_STATUS.md`](V4_C2_COS_DIAG_125_STATUS.md)。  
**⚠️ cos diag 复核（2026-08-18 同日，读表 + 读码）—— 判定「不签」不变，但四处要改、一处结论被推翻**：  
   1. **`clip_insufficient` 这个标签自相矛盾，应记为 `unclassified`**。该行是合取式「① FAIL **且** 首动作 cos<0」，cos 实测 **+0.806/+0.762** ⇒ 第二项**假** ⇒ **该行不成立**。观测**落在事前三行之外**（本节洞 1 当初已预言）。⇒ §4.1 事前表在本轮 **未覆盖**（`unclassified`，事前表不改写、见提案 §4.1 注记）；「不签 §4」的依据是 **cos-diag 那张后续事前表**（`V4_C2_COS_DIAG_125_PROMPT.md`），不是 `clip_insufficient`。凡写「判定 `clip_insufficient`」处均按此读。  
   2. **本跑最强的数是 `cos_path_goal` = +0.075 / +0.098（≈85°，负 ep 3/7 与 2/8），不是 first-act cos**（actor +0.81/+0.76 ≈37° 偏，heur **+0.999** ≈2°）。形态 = 「t=0 朝前、整条路径几乎垂直于目标」，恰好命中收紧块里写的「t=0 正、沿 t 翻负 ⇒ 跟踪丢失」那一支。**仍不签**，但理由必须换成**洞 4**：训练侧只有一个 `_mock_goal_episode()`、评测侧 |az|≤0.8° ⇒ 两侧 goal 同一方向，goal concat 进 actor 是**常量输入、可被 bias 完全吸收**，本 harness 上表达力增量 **0**。不写死这句，将来会有人拿 `cos_path` 负 ep 重开 In 表。  
   3. **Pearson 不可用**：r=+0.588（n=7 ⇒ p≈**0.17**）、r=+0.217（n=8 ⇒ p≈**0.61**），两跑差 2.7× ⇒ **无证据**，下一案不得建立在它上。**`mean_real_minus_imagined ≈ −91` 混了 horizon**（想象 15 步 vs 真实整局 ≤200 步），不是校准误差；要量化须取**同窗口**（真实前 15 步）。  
   4. **RH 在可实现臂上高估 progress ≈4.2×，且倍率随臂变 ⇒ 倒挂在想象内部即可复现**（`w_progress=1.0` 米制；Σmaneuver −0.15 = 15×1.0×0.01 反证权重已施加）：(b) 最大前飞 yaw**恒 0** ⇒ `advance_goal_rel_body` 记账精确、15 步×1.0 m = **恰好 15.0 m** 闭合（`‖goal_rel‖` 30→15.0 印证），而 RH `Σprogress` = **+62.60** ⇒ **4.2×**；(a) C2 π 闭合 12.2 m vs **+81.64** ⇒ **6.7×**。**几何排序**（前飞 15.0 > π 12.2）与 **RH 排序**（π 81.64 > 前飞 62.60）**相反**。⇒ **不需要真机**：倒挂在**可实现集合内**、在想象内部已经存在。  
   5. **由 4 推翻**「多出来的 λG0 来自侧向/垂向自由度、不要读成再开 RH 案」（§A STATUS C2 训后节）：**目标正前方时前飞就是闭合最优**，侧向/垂向不可能多闭合一米 ⇒ 该盈余只能来自 **RH 校准**。**RH 案重开**，依据比当初作废的 A.3 硬（A.3 因臂不可实现而废；(b) 完全可实现）。  
   6. **code lead（未修，勿顺手改）**：`advance_goal_rel_body`（`goal_features.py:60-73`）只做 `g[:3] -= a[:3]`，**不按 `a[3]`（yaw delta）旋转** body-frame goal，而 `imagine()` 正是用它逐步传播 RH 的 conditioning（`imagination.py:164-165`）⇒ 任何 yaw≠0 的臂，喂给 RH 的 goal 方向与幅度都失真（π 有 yaw、(b) 无 ⇒ π 的 12.2 m 本身也不可靠，(b) 的 15.0 m 不受影响）。这是「倍率随臂变」的具体候选机制。改它会改 §A 全部数字 ⇒ **走签字**。  
   7. 小项：`goal_rel0` up 分量 **0.07–1.04**（非 0，疑为起飞悬停高度与 spawn z 之差，**待一句确认**，非 harness 异源）；我在 PROMPT 里加的 `cos_mean10_act_xy_goal_body_xy` / `mean10_action` **未报**，JSON 里有，零成本可补。  
**⇒ 下一件**：**不是**签 §4 In 表、**不是**再训现 π。须另案查想象-真实倒挂（WM 逐步转移保真 / 评测 z0 与训练 z0 域差）；全权 ① 仍须先解**评测期**丢局（**不**降 `n`）。  
   > **更新（2026-08-18 复核后，收窄到更便宜的一步）**：倒挂**不必**先去查 WM 转移 / z0 域差 —— 上面第 4 条已证它在**想象内部、可实现集合内**就存在（(b) 臂几何 15.0 m vs RH +62.60）。⇒ ~~下一件 = **RH 校准曲线**~~ ← **DONE（2026-08-18，125 离线）**。脚本 `v4_rh_progress_calib.py`；JSON `artifacts/v4_rh_progress_calib_c2train_20260818.json`。逐步 `out.progress` vs `Δ‖g‖`（≡ `analytic_progress`，stub 运动学）：**不是 1:1**。π **6.66×**（81.64 / 12.26）；max-forward **4.18×**（62.60 / 14.99，yaw≡0 ⇒ 不是漏转 yaw）；retreat **wrong sign**（RH +0.62 vs analytic −1.00）。形状：t0–t4 近校准，**t≈6 起 RH 爆到 6–9 m/步**而几何仍 ~1。事前规则（forward |ratio|≥2 或 retreat 反号）⇒ **`sign_reopen_rh_progress_head`**。a4 与 a23 重叠臂逐位相同。**不**回到 WM/z0 那条更贵的路（曲线未近 1:1）。改 RH 仍须签字；代码未动。  
**⇒ 下一件**：R1 **DONE**（2026-08-18 HEAD `743c4de`）—— calib **1:1 PASS** → H100 从零重训 `v4_ac_ckpt_20260818_c2_analytic_progress` → 125 ① 再 gate **仍 FAIL**（n=5 非全权；seed0 **−2.65** / seed1 **−1.38** vs heur ~8–10）；④ **PASS**。imagΣG **~4**（was ~85）。**不签** §4 In 表。详见 [`V4_RH_REOPEN_125_STATUS.md`](V4_RH_REOPEN_125_STATUS.md)。  
**全权 ① 的前置**：先分清扫描拒收 vs `_run_one_resilient→None` 再修（**不**降 `n`——「不降阈值凑过」）。  
**（裁定当时原文，2026-08-18，保留存审计链）动作空间一致性裁定 = C2**：策略分布改为 `u~N(mean,std)`、`a = limits ⊙ tanh(u)`（`limits = body_delta_limits(1/env.step_hz)` = 部署同一盒），logp 加 tanh 雅可比 ⇒ 采样律**结构性**落在部署动作集内、REINFORCE 无偏。~~**下一件 = 重跑 §A/§A.3/§A.4（`n_action_clipped` 应为 0）→ H100 从零重训（禁 warm-start 现 ckpt）→ 125 ① 再 gate（`n≥8`，判据 `clip_helped`/`clip_insufficient`/`spurious_pass` 已事前写死于提案 §4.1）**。~~ ← **已做，见上句。** 下段保留裁定当时的推理原文：  
（裁定材料，2026-08-18，**原文保留**）：**A.4 的判定不变，但我在事前表里写的处置「在 `imagine()` 落 clip」写法不足** —— actor 更新是 **REINFORCE**（`corrector.py:176` → `actor_critic.update`，`logp = Normal(mean,std).log_prob(rollout.actions)`，`actor_critic.py:257`/`:271`，无梯度穿 `dynamics.step`），字面 clip = 用**未夹高斯**给**夹后动作**算 logp ⇒ (i) 似然错配、REINFORCE 有偏；(ii) 探索塌缩 —— `_log_std`=−0.5 ⇒ σ=**0.6065** 四维同值，是后三维上限（0.4/0.4/0.314）的 1.5–1.9 倍，四维**全在盒内**概率仅 **8.6%**（mean=0），按现 ckpt mean 为 **3.4e-6** ⇒ 样本全是盒面原子、状态内动作方差≈0、**学不出方向**。故裁定项改为三选一：**C1 字面 clip（不推荐单独落）/ C2 有界策略分布 `a = limits ⊙ tanh(u)` + 雅可比修正 logp（推荐；改策略类 ⇒ 须从零重训，禁 warm-start 现 ckpt）/ C3 pathwise 重写想象（本周期不做）**。~~未签字前**不改** `imagination.py` / `actor_critic.py`~~ ← **已签 C2，代码已改（见 §3 顶条）**。  
**处置**：In 表修订仍在提案里但 **当前不充分**；~~**不**开 RH 案（A.3 依据已撤，A.4 确诊不是 RH）~~。不加长训、不降 `δ_p`、不翻 yaml。待签字稿：[V4_SIGNAL1_STRUCTURAL_REFREEZE_PROPOSAL](V4_SIGNAL1_STRUCTURAL_REFREEZE_PROPOSAL.md)。  
   > **更正（2026-08-18 复核）**：划掉句里的「**不**开 RH 案」**已被更强证据推翻**（见上面复核第 4/5 条）。当初两条依据的射程被我写宽了：A.3 作废只说明**那条臂不可实现**；A.4 `fwdmax_ge_pi` 只说明**倒挂不是由 RH 的方向偏好造成**（方向排序在夹后是对的）。两者都**没有**检验 RH 的**幅度校准**，而 (b) 臂（完全可实现、yaw 恒 0）给出 4.2× 高估 + 排序反转 ⇒ **RH 案重开**。校准曲线 **DONE**（非 1:1）= 签字材料；改码仍须签字。  
**另记**：`p_coll≈6e-4` 横跨所有臂（含 1 m/步 15 步头对头撞墙）⇒ 想象里碰撞头近乎**是死的**，④ 想象刹车同样无信号（另案）。A.2 两次跑无 seed，结果漂 ~6%（前飞 λG0 47.02→49.65）；`--seed` 已补。  
`enable_policy_update` **仍 false**。  
**Governance**：2026-08-17 frozen `n_eval_episodes=8`；V4 `n<8` → `authoritative=false`（洞 1 已关）。

### 1.1 判据本身已被立案（2026-08-18，待签字）

**上面全部 FAIL 归因都建立在「① / ④ 是对的判据」这个前提上 —— 该前提 2026-08-18 被推翻。** 立案稿：[`V4_CRITERIA_REFREEZE_PROPOSAL_20260818.md`](V4_CRITERIA_REFREEZE_PROPOSAL_20260818.md)。三条硬结论：

1. **旧 ① 奖励撞墙**。`progress_sum = ‖g−p₀‖ − ‖g−p_final‖`（从位置算，确实不被 RH 污染），但 `p_final = ep[-1].next_obs.position` 且 collector 遇 `done` **break** ⇒ 直线撞墙 = 停在**离目标最近点** = progress 拿满（heuristic 每局 ~8.7 就是这么来的）；而正确的绕行行为**横向位移不计分**。
2. **旧 ④ 可被横漂空过**。横漂策略永不接近障碍 ⇒ on-rate 0、`n_contact=0` ⇒ ④b `before_vacuous` + ④c **0.000** ⇒ **自动 PASS**。M5c/M5d/M5e 的「④ PASS」与「① FAIL」**是同一个病的两面**，不是两个独立结论。
3. **⇒ ①④ 互为对抗**，merge 的 AND 假设「两个独立约束」不成立。唯一同时满足两者的行为是**逼障（贴障绕行）**，而它被 shield 触发深度 **3.0 m** vs ④c 度量带 **1.5 m** 挤成 1.5 m 窄带、且 shield-ON 语料里**根本不存在**。

**新口径（提议）= 分层到达**：探针把起点标成 **S_open**（直线可达 ⇒ 判不退化）/ **S_blocked**（直线撞 ⇒ 判逼障到达），merge 取**合取**；另加两条前置门 **V4-⓪**（近带深度 `D_gt ∈ (0,3.0]`）与 **V4-⓿**（想象排序一致性 —— 设计 §1.3 列了「多步预测误差」为成功指标，但 V0/V1/V4 信号表里从无一条测它）。

**注意**：这**不是**降阈值凑过 —— §4.1 阈值一字不动，新表在两层上都比旧表严，且新增两道门。`enable_policy_update` 保持 false。与正在跑的 **R1 正交，不打断**（R1 = 提案 §2 的 P0）。

**签字现状（2026-08-20，第九轮后）**：权威口径 = 提案 **§4.6（含 4.6.8–4.6.12）**，**唯一可签表 = §5.0，现为 16 行**（第九轮 W1/W2 由 14 行扩表，新增 **5al**「起点集互斥口径更正 + 5ag 改配对检验 + 跑前冻结 `Δ`」、**5am**「shield v5 条件化 / P3.5 本周期 N/A / 无 v5 代价如实登记」，**两行均为阻塞项**）。~~**16 行裁定栏全空 ⇒ 未签字**；`5af` / `5ag` 必须勾 **A 或 B**（空白 = 非法签字），选 5ag(A) ⇒ **5al 必须同时采纳**。**下一动作是人填表，不是再开一轮改判据。**~~

**✅ 签字完成（2026-08-20，用户逐行裁定，16/16 行）**：14 行「采纳」+ **`5af` = (a)** + **`5ag` = (B)**；`5aj` 打包内的 `5p` 二选一同日裁定（到达 = `min_t dist ≤ 3.0`、主动终止 out of scope）；**两个阻塞项 `5al` / `5am` 均采纳**。**判据自此冻结** —— 实施中改任何阈值 / band / `n` / primary-secondary 划分都须 **re-freeze 重签**。**`enable_policy_update` 未因签字翻开**（红线：四信号全过前不翻）。**签字未下调任何阈值。**

**签字随附的三条「主张范围」声明（本文件结论栏正式登记，不得外推）**：
1. **V4-MVP 不主张「想象 AC 有增益」**（5ag(B)）—— 只主张「**不退化 + 安全**」。理由：5c 已定 `P7 planner 到达率 = 经验可解性上界`，`Δ=0` 时 (A) 等于要求 actor 打到该上界，且门槛是跑后才知的随机量（违反 5n/B3 的「OC 跑前冻结」）。⇒ **V4 不对「世界模型想象是否有用」出证**，该主张留 **V5 另案**（届时按配对检验 + 跑前冻结 `Δ` 预注册）。
2. **到达能力主张仅覆盖机体前向扇区**（5af(a)）—— **侧向 / 后向 goal 本周期不测**，In-表改动属 V3 另案。⇒ V4 的 ①′ 结论**不得外推为全向导航能力**。
3. **「主动终止」本周期 out of scope**（5p 后半）—— policy 自主停止的能力**一条判据都没有**，如实登记为最终目标的未覆盖维度。

**签字后仍存在的「等实测才能填的空」（是填空，不是待裁）**：`[lo,hi]`（等 ⓪f）、`δ`、`θ`（等 P7-diag，只在 `S_diag` 上拟合）、`Q_0.25(C_P7)`、C1 的进带前窗口 `k`（须与 5ac(a) 的进展窗口 `k` 一并冻结）。**`Δ = 0` 已冻结在案**，因选 (B) 暂无适用对象。

**签字同时如实登记的最大风险（5am(d)）**：v5 不启用 ⇒ **一次触发即整局 latch、本局报废**，而 τ 通道在 `v = 5 m/s` 下触发到 **≈5 m** ⇒ **①′a-b ≥ 0.50 的经验可解性完全押在 P7-accept 上**（G1 只论证了带定义不与罩互斥，**未论证到达率**）。P7-accept FAIL ⇒ 走 **P7-FAIL 下车站**（5ai），**不得就地放宽阈值、不得顺势启用 v5**。

---

## 2. 里程碑

| 步 | 内容 | 状态 |
|---|---|---|
| M0 | 设计规格入库 | ✅ |
| M1 | actor_critic + 单测 | ✅ |
| M2 | corrector 接线 + smoke | ✅ `v4_ac_smoke.py` |
| M3 | H100 短训 ckpt | ✅ PASS — `v4_ac_ckpt_20260816/` (stub, superseded) |
| M3b | H100 WM encode 300-iter train | ✅ — `v4_ac_ckpt_20260816_wm/` latent_dim=1536 |
| M4 | `_v4_gate` self-check | ✅ |
| M5 | 4090 ①④ eval (stub) | ❌ merge FAIL — ① −13.54 vs heur 9.71; ④ PASS |
| M5b | 4090 ①④ re-gate (torch WM, legacy RH) | ❌ merge FAIL — ① −68.88; ④ v4_hard 0.143 vs v1 0.00 |
| M5c | reward-head finetune + AC `*_wm_rh` + re-gate | ❌ merge FAIL — ① −3.17 vs heur 7.44; ④ **PASS** (0.143 vs v1 0.25) |
| M5d | goal inject + real RGB z0 + re-gate | ❌ merge FAIL — ① −8.74 vs heur 8.42; ④ **PASS** (0.000 vs v1 0.429) |
| M5e | **C2 有界策略分布**（动作空间一致性） | ✅ 代码 + 从零重训；① **仍 FAIL**（n=5 非全权）；④ PASS；判定 `clip_insufficient` |
| M6 | flip yaml | **禁止**（merge 未 PASS） |

---

## 3. 变更记录

- **2026-08-20（**新建 V4 执行 runbook**）** — 改了什么：新增 [`experiments/aerial/RUNBOOK_v4.md`](../../experiments/aerial/RUNBOOK_v4.md) —— 前提链 P0→P8 逐步表（**下一步 = P0c**、**P3.5 记 N/A**、P7 两趟 + 下车站）、四信号执行口径、**§3「跑前必须先判断/先冻结」16 项**（其中 `[lo,hi]` / `θ` / `k` / `Q_0.25(C_P7)` / primary 划分 / OC 曲线 / spare 池大小 / 冻结清单数值 = **待填**，其余已签死）、**§4「必须实测不许假设」13 项**、落盘契约（缺字段 ⇒ `authoritative=false`）、主张范围三条、红线、已知残余。为什么 / 依据：签字后判据已无未决二选一，但「跑前要冻什么 / 要先测什么」散在九轮复核（§4.6.8–§4.6.12）里 ⇒ 收成两张可勾清单，避免实施时漏冻结而事后定阈值（撞红线）。**本文件不新增、不修改任何判据**，定义一律以提案 §4.6 为准；代码 / yaml 未动，`enable_policy_update` 仍 false。
- **2026-08-20（**判据签字完成**：§5.0 削减签字表 16/16 行裁定填毕 ⇒ V4 判据 re-freeze 生效）** — 改了什么：把 §5.0 十六行的裁定栏全部填上（14 行「采纳」+ 用户手选的两处强制二选一），并把三处被裁定的口径**写死进 §4.6 正文**。为什么 / 依据：第九轮已把签字表补到 16 行且两个阻塞项（5al / 5am）处置就绪，第八/九轮都写明「下一动作是人填表，不是再开一轮改判据」⇒ 用户逐行裁定。逐项：
  - **14 行采纳**：13（§4.6 = 唯一权威口径，`n` = 每层 16 随之冻结）/ 5z（修，头号阻塞：in-band = 三通道并集非介入 ∧ `engaged=false`，`[3.5,5.0]` 撤回）/ 5aa（⓪f）/ 5ab（修，跑前分叉）/ 5ac（修）/ 5y / 5ad / 5ae / 5ah（P0c 丢局修复，禁降 n）/ 5ai（P7-FAIL 下车站）/ 5aj / 5ak / **5al**（阻塞解）/ **5am**（阻塞解）。
  - **`5af` = (a)**：评测 goal **限机体前向扇区**，In-表本周期不改；**如实登记「侧/后向 goal 不测」**⇒ ①′ 结论不得外推全向。已落到 §4.6.1 新增「评测 goal 分布」行。
  - **`5ag` = (B)**：**V4-MVP 不主张「想象 AC 有增益」**，只主张「不退化 + 安全」；宣告已写入本文件 §1.1 结论栏。(A) 留 V5 另案。
  - **`5p`（在 5aj 包内）**：到达定义写死 **`min_t dist(p_t,g) ≤ 3.0 m`**（**不改 harness 控制流**，`max_steps` 仍跑满、路径长在 `T_arr` 截断）；「**主动终止**」**明确宣告 out of scope**。两条均已落到 §4.6.3 / §4.6.1。
  - **5al 生效范围校正**：(a) 起点集口径（`S_diag` ⟂ {`S_accept` ∪ actor gate}、`S_accept` ≡ `S_gate`）**与 5ag 选支无关**，因 E3 的 `P ⊆ S_accept` 本身就要求 actor 飞同一批起点；(b) 配对检验因选 (B) **暂无适用对象**，保留 conditional，`Δ=0` 已冻结、日后不得另议。
  - **红线复核**：签字**未下调任何阈值**（5ag(B) / 5af(a) / 主动终止 out-of-scope 都是**缩小主张范围 + 如实登记**）；**`enable_policy_update` 未翻**；shield 控制律未动（v5 仍 conditional、P3.5 仍 N/A）；**代码 / yaml 一行未动**。
  - **自此起的纪律**：判据冻结 ⇒ 实施中任何阈值 / band / `n` / primary 划分的改动都属 **re-freeze**，须重开表签字，禁止就地调。

- **2026-08-20（第九轮复核：复核对象 = 第七/八轮**自己的处置** → §4.6.12 W1–W7；签字表 **14 行 → 16 行**（+5al / +5am）；**第八轮「可以按 14 行签」的结论失效**）** — 改了什么：**(W1，阻塞)** 第七轮 U4 引入的硬约束「actor gate 起点集与 `S_diag` / `S_accept` **三者互斥**」与 E3 的固定公共子集 `P`（= P7-planner 到达的起点）冲突 —— planner 验收趟跑在 `S_accept` ⇒ `P ⊆ S_accept` ⇒ actor **永不飞 `P`** ⇒ **①′c-b / ①′d-b 中位数无样本可算**；且与 5ag(A)「同 `S_accept`、按 start 配对」直接矛盾 ⇒ 就地改为 **`S_diag` ⟂ {`S_accept` ∪ actor gate}** 且 **`S_accept` ≡ `S_gate`**（训练集仍与三者不交，5m/B2 不动；θ 仍**只在 `S_diag` 上拟合** ⇒ 防 p-hacking 效力不减，**未放松任何阈值**）。**(W1-b)** 5ag(A) 的阈值 `arrival_rate_P7planner` 是 P7-accept 跑完才知的**随机量** ⇒ 违反 5n/B3「OC 曲线跑前冻结」⇒ 改为 **`arrival_actor ≥ arrival_planner − Δ`**，`Δ` 跑前冻结（提议 **0**）+ **McNemar / paired bootstrap** 判读（`Δ=0` 与原式等价 ⇒ 不放松）。**(W2，阻塞)** §4.6 仍在四处依赖**已被 5r′(改) 降为 fallback 的 shield v5**：§4.6.1 评测构型写「shield-on（v5）」、冻结清单含 v5 的 `release_depth_m/M/上限`、**§4.6.6 P3.5「shield v5 回归」标「必须」**、§5 项 5 的 U1 注记 ⇒ 全部改 **conditional**：评测构型 = **现行第 (4) 代（latch + 有界状态反馈后退）**、v5 三参数**本周期不冻结**、**P3.5 = N/A**，并**如实登记代价**：无 v5 解锁 ⇒ **一次触发即整局 latch ⇒ 本局报废**，τ 通道在 `v=5 m/s` 巡航下触发到 **≈5 m** ⇒ **①′a-b ≥ 0.50 的经验可解性完全押在 P7-accept 上**（G1 只论证过带定义，从未论证到达率）；若 P7-accept 在 S_blocked FAIL ⇒ 走 **P7-FAIL 下车站**。**(W3)** 5q 记「已被 §4.6 吸收」，但 **C1（进带前 k 步的 `D̂_fovmin` / `v_fwd` / 侧向位移）** 与 **C2（`S_open` 的 `intervention_rate`）** 从未进 §4.6.1 落盘契约 ⇒ 契约自检会判 `authoritative=false`（同 U1-b 类）⇒ 已补入契约并归到 **5aj**。**(W4/W5/W6/W7)** 抬头警示行与 §1.3.5 的「§6 P1」改 **Q1**；§6 表头正文改 **Q1–Q5 / 五条**（Q5 是新增、非改名）；§5 归属清单补漏列的**项 7 / 项 8**；重复的 `5r′` 原行补「不签、无需裁定」注记。**W1–W7 全部成立，无一条不成立。** **本轮无一处放松阈值；代码 / yaml / 阈值 / flag 仍一行未动。** **⇒ 下一动作：人填 §5.0 的 16 行**（5af / 5ag 必须勾 A 或 B；选 5ag(A) ⇒ 5al 必须同时采纳），不是再开一轮改判据。

- **2026-08-19（第八轮：将第七轮后独立检查入库 → §4.6.11 V0–V5）** — U1–U9 **已闭合、不再自毁**。削减表 14 行裁定栏**仍空** ⇒ 下一动作是人填 §5.0（尤其 **5af / 5ag**），不是再改判据。入库：P7-diag 先冻 `[lo,hi]` 再同 log 算 θ；P2 完成判据含 `wm_out` 接线（`collector.py:185` 现状未传）；§2 P0 IN PROGRESS 标 stale。已知残余不解。**代码 / yaml / 阈值 / flag 仍一行未动。**

- **2026-08-19（**第七轮复核 —— 复核方 = 用户，第一次非提出人复核**：新增 §4.6.10（U1–U9）+ **§5.0 削减签字表 v3** + 签字项 5ad–5ak；**旧签字表整表降为审计留档、禁止整表签**；立案标题改「分层到达；逼障仅当 ⓪f 证出可行带」）** — 改了什么：提案新增 **§4.6.10「第七轮复核（U1–U9）」**（九条 + U1-b + 「若不做 U-系列处置就签字会发生什么」净影响表）与 **§5.0 削减签字表 v3**（14 行：13 / 5z(修) / 5aa / 5ab(修) / 5ac(修) / 5y / 5ad / 5ae / 5af / 5ag / 5ah / 5ai / 5aj / 5ak）；并就地更正：**标题**改为「目标改为**分层到达；逼障仅当 ⓪f 证出可行带**」（原标题留审计）；§0.2 加「本节结论已被第五～七轮推翻两次」注记；§3.2 加 supersede 注记；§4.6.1 落盘契约补 **5d 撞点 7 项 / 5e `action_commanded` / `_breached` 三通道布尔位 / τ dt 直方图**；§4.6.3 的 in-band 行**重写**、新增 **U3-b（band 冻结时序）** 与 **θ 两趟协议**行、分叉行的 `hi` 换操作定义；§4.6.6 **P0 改 DONE**、新增 **P0c（丢局修复）** 与 **P7-FAIL 下车站**、P7 拆 **P7-diag / P7-accept**、P8 加 goal 扇区前置条件、P1 的 forward-ratio 降诊断、P2 加 band 时序连带；§4.6.7 溯源表加 12 行；§4.6.9 末尾加「本节两处已被第七轮就地更正」注记；§5 旧表加逐项归属 blockquote + 对 **5 / 5k / 5f / 5l / 5x / 5r′ / 8 / 10** 七行加「不签 / 改口径」注记；§6 流程补丁 **P1–P4 → Q1–Q4** 并新增 **Q5（判据自身须经非提出人复核）**；§7 的 R1 从「IN PROGRESS」改 **DONE**。**代码 / yaml / 阈值 / flag 仍一行未动。**

  为什么（U1，**本轮头号，实施第一天必踩**）：抬头写「以 §4.6 为准」，但 §5 仍是一整张「提议 = 采纳」表 ⇒ **签「全表采纳」会把第六轮刚撤回的东西重新冻回去**：项 **5**（①′ 在 shield-on 下测 —— 原稿口径，D1 已否）、**5k**（带占用按速度方向 —— 5u/G2 已撤）、**5x**（仍写死 `[3.5, 5.0]` —— H1b/H3 已撤）、项 **10**（带占用 `θ=0.10` —— H5 已失锚）；且 **5r′** 仍写「必须最先裁」而抬头说头号是 5z；§6 的 **P1–P4** 与前提链 **P0–P8** 撞名（同一文档里「P1」有两个意思）。⇒ 处置 = **§5.0 削减签字表 v3 为唯一可签表**，旧表逐项标「内容已被 §4.6 吸收 / 被 v2 supersede 不签 / 改由 5aj 打包 / 改由 5ak 处理 / 改由 5ag 强制表态 / 另办」。**U1-b（本稿自补）**：逐项核对发现 **5d（撞点 7 项）与 5e（`action_commanded` 落盘）在 §4.6.1 的落盘清单里根本没有** ⇒ 只签项 13 会**静默丢字段** ⇒ 新增 **5aj** 打包签 5c/5d/5e/5g/5o/5p 并把 5d/5e 补进落盘契约。

  U3（**第二个必踩项，且是第六轮我自己新引入的缺陷**）：第六轮的 in-band 谓词写成「`clearance ∈ [lo,hi]` ∧ `τ̂ ≥ min_tau_s` ∧ `engaged=false`」，与 `_breached`（`safety.py:96-107`）比有三处不等：① **漏 `p_coll > 0.5`** 这条通道；② D̂ 通道被含混写成 `d̂_fwd`，实现是**全 FOV min**（`depth_predictor.py:81-90`）；③ **`engaged=false` 不能替代「本步未破」** —— `engaged` 是 latch 状态，「本步刚破、尚未 latch」时它仍为 false ⇒ 该步会被计入 band，而它恰是罩即将接管的步 ⇒ 与 F3「接管步不计」同源自相矛盾。**桌面副本比仓内更松**（连「d̂ 不触发」都没写）⇒ 两份文本不一致。修 = **`NOT _breached(step)`（三通道逐字展开）∧ `engaged == false`**（两条**合取**，不是替代），并落盘**三通道各自的布尔位**（否则带为空时无法归因）。**U3-b（连带）**：`p_coll` 现在是死值（P2 未做）⇒ 第三通道恒不触发 ⇒ 现测的「非介入域」是**乐观上界** ⇒ **band 正式冻结必须排在 P2 之后**，或标 `band_frozen_before_p2=true` 并在 P2 后强制重跑 ⓪f + 重裁 5ab 分叉（**只许缩窄，放宽须单独签字**）。

  U9（**第三个必踩项**）：评测期丢局仍不在前提链上。现况 `n=5 < 8` 出在 `v4_gate_run_partials.py` 的 `_run_one_resilient` 路径（`if ep_on is None: continue` 直接吞掉），而 §4.6.1 已把 `n` 收紧到**每层 16** 且 ④ 仍**on/off 配对**（任一臂丢 ⇒ 整对废）⇒ 不先修丢局，**16 长期填不满** ⇒ 全部信号 `authoritative=false`，叠上红线「不降 n」= **gate 在结构上永远不可能全权**（四信号全过这件事变成不可能事件）。修 = 新增前提步 **P0c**（排在 P1 **之前**）：三计数器分类落盘（`n_invalid_spawn` / `n_none_returned` / `n_pair_broken`）+ **预留 spare 起点补扫到冻结 n**（spare 清单与消耗顺序跑前落盘，禁临时新采）+ 仍不足 ⇒ `authoritative=false` **且必须报三计数器** + **明令禁止用降 n 解决**。

  U4（**第二处第六轮自引缺陷：自指**）：H5 写「θ 由 P7 planner 在同一 band 下的自身占比 + 裕度推导」，而 §4.6.6 的 **P7 验收 = §4.6.3 全判据（含 ①′d-b，含 θ）** ⇒ 循环定义 ⇒ 实际会变成「用第一次正式 P7 的占比当 θ，再宣布这次 P7 过了 ①′d-b」= **在同一批数据上又定阈值又验收**（等价事后调阈值，**撞红线**），且 P7 构造性通过。修 = **两趟协议**：**P7-diag**（诊断集 `S_diag`，θ 未定义、只落盘 `band_frac` 与 clearance 分布 `C_P7`）⇒ **冻结 θ / 裕度式 / k / `[lo,hi]`（加时间戳）** ⇒ **P7-accept**（在与 `S_diag` **起点 + env seed 双不相交**的 `S_accept` 上验收）；`S_diag` / `S_accept` / actor gate 起点集**三者互斥**，任何重叠 ⇒ `θ` 记 `authoritative=false`。

  U2（**产品承诺**）：5ab 的跑前分叉会**抽空立案标题** —— 若 ⓪f 证不出可行带（以晚⁷ 近带 `P(over-read)=0.377`、巡航 τ 触发到 ≈5 m 看**很可能**），①′d-b 降 secondary ⇒ merge 只剩「到达 + 效率 + 不撞」，而标题仍写「目标改为**逼障绕行**」⇒ 签完字名实不符。修 = **标题与 §0.2 同步改**（已改）。且第六轮拍的 **`hi ≤ 6 m` 与「逼障」没有任何绑定**（不是罩参数、不是场景几何、不是 planner 行为的函数）⇒ 换成**操作定义** `hi ≤ Q_0.25(C_P7)`（`C_P7` = P7-diag 里 planner 在 S_blocked 实际经过的 clearance 分布 ⇒ 「逼障」= **比经验可解参考轨迹的多数行为更贴障**）+ 绝对护栏 `hi ≤ 8 m`（**如实登记为拍的**，仅与 ⓪f 的 support 上界 `(3.0, 8.0]` 对齐）。

  U5 / U7 / U8 / U6：**U5** —— 项 8「多样 goal」不得与「本周期不改 In 表」同时签：`π(a|z)` **goal-blind** ⇒ 侧/后向 goal 上 **①′a-o 预先注定 FAIL**（用一个已知不可满足的判据去否证 actor）⇒ 改**条件项 5af**（评测限 body-forward 扇区 + 如实登记范围收窄 / 或 In-表前移，后者**属 V3 须另案**）。**U7** —— 5l 写「二选一、不得沉默」而**至今沉默**；F7 盲对照臂测的是「gate 是否依赖视觉」，与「想象 AC 是否有增益」是**两个不同的反事实**，不可替代 ⇒ 新增 **5ag（=5l′）强制表态**：(A) 预注册 `arrival_rate_actor ≥ arrival_rate_P7planner`（同 `S_accept`、按 start 配对）入 primary merge，或 (B) 宣告 MVP **不主张**该增益，**空白 = 非法签字**。**U8** —— 5q 把 C7（预注册否证停止规则）算进「采纳」，§4.6.7 又把 **R-16** 列为未收紧，而前提链 v2 表里**没有任何「FAIL ⇒ 停」的节点**（梯子只有上行台阶、没有下车站）⇒ 新增 **P7-FAIL 下车站**（不进 P8 / 判「V4-MVP 前提被否证」/ 退回 WM-感知 / **禁三种反应**：降阈值、换易起点、只判 S_open），**R-16 改「部分闭合」**并登记剩余缺口（⓪ / ⓿ / P3.5 各自 FAIL 后的停止规则仍未预注册）。**U6** —— R1 已 DONE（`30b9ff8` "calib PASS, analytic AC retrain, gate still FAIL"；`d96da1d` "wire analytic progress in imagine aux path"）⇒ (i) §4.6.6 **P0 改 DONE**；(ii) **5f 的护栏比值恒 ≈1** —— aux 路径上 imagined ΣG **就是** analytic ⇒ 护栏空转 ⇒ 改为「对**真实 rollout 的 Δ‖g‖**（离线回放）比」或撤案（**5ak**）；(iii) **§3.2「横漂只能用 RH 谎报解释」被削弱** —— 谎报通道已关（校准 1.00）而 **① 仍 FAIL** ⇒ 排除式论证作废，至少还有一个未识别成因；(iv) P1 的「forward ratio ∈ [0.8,1.2]」**降诊断**（actor 已不吃该读出）；(v) `PROJECT_STATUS.md:30` 的「R1 已签，125 IN PROGRESS」**stale，已更正**。

  流程侧（Q5，本轮的元结论）：提出人自查**连续四轮**（三/四/五/六轮）每轮都在自己刚写的文本里找出致命项 —— 有效，但**收敛慢且有盲区**：U3 / U4 两处是**第六轮自己新引入**的。⇒ §6 新增 **Q5「凡 re-freeze 类提案，签字前必须有一轮非提出人复核」**，本轮（用户）即第一次执行，可计入项 12 的**部分**履行。**头号阻塞签字项仍是 5z（按第七轮修订文本）**；`enable_policy_update` 保持 **false**。

- **2026-08-19（对 5x 自身的第六轮复核：新增 §4.6.9（H1–H7）+ 签字项 5z / 5aa / 5ab / 5ac；**5x 的方向被实测加强、数字与依赖链撤回**；头号阻塞 5x → 5z）** — 改了什么：提案新增 **§4.6.9「第六轮复核（读码 + 实测记录，H1–H7）」**（七条 + 「H1–H3 对 5x 的净影响」表），并**就地更正**：§4.6.1 加「**τ 的 dt 自检**」行；§4.6.2 新增 **⓪f**（反应带外侧精度 + D̂/τ **双通道误触发率**）并把重跑触发扩到 ⓪f；§4.6.3 的 ①′d-b 换成「`band_frac ≥ θ`（数字待定）」并新增四行（**并集非介入** / **速度条件** / **band_frac 改进展加权与路径长加权** / **θ 须重导**），带边界校准行的依赖源从 ⓪a 改 **⓪f** 且 `[3.5,5.0]` **撤回**，新增「**跑前分叉**」行；§4.6.6 的 P3 扩范围含 ⓪f；§4.6.7 溯源表加 9 行 + 一条「**实现现状**」注记；§4.6.0 顶部与 §4.6.8 的 G1 格加 supersede 注记（**原文不改写**）；签字栏新增 **5z / 5aa / 5ab / 5ac**，**5x 标注「方向留、数字撤」**；头部修订记录加第六轮。**代码 / yaml / 阈值 / flag 仍一行未动。**

  为什么（H1，**本轮头号**）：第五轮的 5x 只对齐了罩的**一条**触发通道。读码确证 `safety.py:96-107` 的 `_breached` 是 **`D̂ < 3.0` ∪ `τ < 1.0` ∪ `p_coll > 0.5`** 三通道**并集**，而 `v4_gate_run_partials.py:250-291` 确证 **τ 通道在 V4 gate 里是活的**（`make_tau_predictor(kind='foe_calibrated', center_frac=0.5, use_gt_depth=False)` + `DepthTauShield(min_depth_m=…, min_tau_s=cfg.safety.min_tau_s=1.0)`）。两通道**几何不同**：D̂ = **整幅 FOV min**（`depth_predictor.py:81-90`），τ = **前向中心裁剪 `center_frac=0.5`** 的 FOE 光流 τ（`tau_predictor.py:143-192, 322-347`）。⇒ 只按 `clearance_fov` 定义的带**不是罩的非介入区** ⇒ in-band 判定必须改成「**并集非介入**」（`clearance_fov ∈ [lo,hi]` ∧ `τ̂ ≥ min_tau_s` ∧ `engaged=false`），否则与 F3「接管步不计入」自相矛盾。**H1b（数字撤回）**：τ = 距离 ÷ 闭合速度 ⇒ 非介入区**随速度收缩**；`action.py:43-45` `MAX_BODY_VELOCITY[0]=5.0 m/s`、`min_tau_s=1.0 s` ⇒ 巡航时 τ 通道**触发到 ≈5 m** ⇒ 第五轮提的 `[3.5, 5.0]` **整段落在触发区内** ⇒ 数字必须撤回，且判据须写明「在 clearance `d` 处不被接管要求 **`v_fwd ≤ d / min_tau_s`**」⇒ **「逼障绕行」在部署构型下的可证明含义再收窄为「减速贴障通过」**，不是高速贴障。

  H2（**依赖链悬空**）：5x 的 `δ`（带下边界）与 G7 的 `release_depth_m` 都定义在 **GT > 3.0 m** 区，但 ⓪a 的 support 是 `D_gt ∈ (0, 3.0]`，晚⁷ diag 的分箱也只有 `<1.5` 与 `[1.5, 3.0)` ⇒ **GT > 3.0 m 的 D̂ / τ 行为无任何实测** ⇒ 「由 ⓪ 校准后冻结」**不可执行**。修 = **新增 ⓪f**：`D_gt ∈ (3.0, 8.0]` 上的 median/p90 AbsRel + **`P(D̂ < min_depth_m | D_gt ∈ [lo,hi]) ≤ 0.05`** + **`P(τ̂ < min_tau_s | d_fwd/v_fwd ≥ 2·min_tau_s) ≤ 0.05`**，逐帧 support 同 ⓪b；**5x 的 δ 与 G7 的 release 改依赖 ⓪f** ⇒ **P3 范围扩大**（P3 不出 ⓪f，5x / G7 / 分叉全部无法裁）。

  H3（**「存在可行带」本身是未证事实** ⇒ 跑前分叉）：晚⁷ 同一份 diag 里近带 `P(over-read)` 从 1.0 降到 **0.377** ⇒ retrain 后的头**既不是一致欠读也不是一致过读** ⇒ 把 `GT 1.5 → D̂ 0.645`（gain≈0.43）**外推到 3.5–5 m 没有依据**；若 gain 真在 0.4 附近，`D̂ ≥ 3.0` 需要 GT ≈ **7 m** —— 那已不叫「逼障」。⇒ **跑前预声明分叉（须在看到数据前签字）**：⓪f 出数后若存在「并集非介入 ∧ `hi ≤ 6 m`」的带 ⇒ 冻结、①′d-b 保持 **primary**；**若不存在 ⇒ ①′d-b 降为 secondary（只报不入 merge）**，机制主张全部落到 **⓿ + P7**。事后再做这个分叉就是 p-hacking，预声明才是合法功效纪律。

  H4/H5（度量与阈值）：`frac_steps_in_band` 是**时间占比** ⇒ 与 H1b 要求的带内减速合起来，最省力的刷分方式是**在 4 m 处慢速磨蹭**（median 又只在到达局上算 ⇒ 「磨蹭完再到达」反而得高分）⇒ 改 **primary 取更严者**：(a) **进展加权**（一步计入 band 还须在 k 步窗口内 `dist(p,g)` 严格下降）、(b) **路径长加权** `band_path_len / total_path_len`（对速度不变），纯时间占比降诊断。`θ = 0.10` 是为「带在 standoff 之内」选的 ⇒ 带一挪，基线占比就变 ⇒ **0.10 已失锚（无定义，既不是收紧也不是放松）** ⇒ 须由 **P7 planner 同定义下的自身占比 + 裕度跑前重导并冻结**；**若重导值低于 0.10 ⇒ 属放松 ⇒ 须单独签字**，不得静默采用。

  H6/H7：**H6（条件性风险，非已证 bug）** `tau_predictor.py:289` 默认 `dt_s=0.1`（10 Hz）而 `configs/aerial_rl.yaml:20` `step_hz=5.0`（dt=0.2），`:330-333` 仅在 `obs.t` 有限且单调时才用差分覆盖 ⇒ **一旦回退，τ 秒数减半、τ 触发面约翻倍 = 悄悄改了部署罩** ⇒ 落盘实测 dt 直方图 + 回退标志，用过 ⇒ `authoritative=false`。**H7（反向，一个担心被排除）**：`configs/aerial_rl.yaml:131-132` `kind: foe_calibrated` / `use_gt_depth: false`，`v4_gate_run_partials.py:258` 硬写 `use_gt_depth=False` ⇒ τ 通道是**纯视觉**，闭环里没有 GT 深度 ⇒ 红线 A2「GT 深度只离线取」与部署罩**不冲突**，无需改动，登记为已核对。

  **实现现状登记**：`band_frac` / `frac_steps_in_band` / `in_band` 在 `v0_metrics.py`、`v4_metrics.py` 里**都还不存在**（grep 无命中）⇒ ①′d-b 的度量是**待实现**，H1/H4 的更正**不涉及改已有代码**。**G1 被实测加强**：晚⁷ 的 `_diag_depth_vs_gt` 实测 full-field `<1.5` 与 `[1.5,3.0)` 两带 `P(trigger)` **均 = 1.0**（retrain 前 0.697 / 0.802）⇒ 「`[1.5,3.0]` 被罩结构性禁止」不只是构型推理。门禁未动：§4.1 阈值（`1.5 m` / `0.80`）、`enable_policy_update=false` 全未改；R1 on 125 不受影响。**头号阻塞签字项：5x → 5z（5x 的方向部分仍在，数字部分作废）。**

- **2026-08-18（对 §4.6 自身的第五轮复核：新增 §4.6.8（G1–G7）+ 签字项 5x / 5y；**头号裁定从「改 shield」改为「改判据的带定义」**；5u 撤案、5r′ 降 fallback、项 6 很可能撤案）** — 改了什么：提案新增 **§4.6.8「本节自身的复核（第五轮，读码确证）」**（G1–G7 七条缺陷 + 「G1 的连带结论」表），并**就地更正 §4.6 内部**：§4.6.3 的 band 定义、①′b-o/①′c-b 的效率公式、盲对照臂、公共子集、§4.6.4 的 ④′c、§4.6.0 的 `release_depth_m`；§4.6.7 溯源表加 5 行、改 1 行；§4.6.0 顶部加 ⚠️ 降级横幅并改标题；签字栏新增 **5x**（头号阻塞）与 **5y**，**5u 划掉撤案**、**5r′ 改「降为 fallback」**、**项 6 改「暂缓，且很可能撤案」**；头部修订记录加第五轮。**代码 / yaml / 阈值 / flag 仍一行未动**（本轮改的全部是我自己**未签字**的 §4.6，不是预注册的 §1/§2）。

  为什么（G1，**头号反转**）：读 `depth_predictor.py:81-90` 确证 **`predict_min` = 整幅深度图（全 FOV）finite&positive 的 min**，而 `safety.py` 的 `min_depth_m=3.0` 就是拿这个数触发。⇒ 我在 §4.6.3 写的「①′d-b band = `clearance ∈ [1.5, 3.0] m`」**是在要求策略违反部署安全罩** —— 且这**与 latch 无关**：即使换成第 (1) 代瞬时罩，进入 3.0 m 以内一样会被接管。**⇒ 第四轮把头号裁定指向 shield 是指错了对象**（这是 D1 上的**第二次反转**：第四轮先被 `safety.py` docstring 推翻一次，第五轮再被 `depth_predictor.py` 推翻一次；两次都以**读码**为依据，不再凭推理翻案）。**最小正确修法 = 改判据，不改罩**：band 整体上移到 standoff **之上** —— `clearance_fov ∈ [3.5, 5.0] m`（provisional），下边界 = `min_depth_m + δ`（δ = ⓪ 实测的 D̂ 近带**欠读**偏差），宽度 1.5 m（= 一个近碰带宽），**P3(⓪) 出数后按式冻结、冻结后不得再动**。⇒ 新签字项 **5x**，shield v5（5r′）**降为 fallback**（仅当将来确有必要测「进入 [1.5,3.0] 的行为」时才启用；本周期**不动 shield**）。

  G2：我第四轮给 D4 开的方子（band = **≥4 方位全向最小净空**，body ±x/±y）**本身可被空过** —— 它引入**相机看不见的方位** ⇒ 「盲区里贴着障碍飞」也算带占用，且与触发几何不一致。依据：`predict_cones(center_frac=0.5)` 虽存在，其 docstring 明写「Not wired into the collector/shield until P0b —— `predict_min` remains the sole input for `depth_min_pred`」。⇒ band 改用 **FOV-min GT 深度**（与 `predict_min` **同几何**），五向 `cone_clearances` 降为**诊断**落盘；侧掠可测性由「带上移」而非「引入 FOV 外方位」解决。**5u 因此撤案**。

  **G1 的连带结论**（范围收窄，诚实登记）：① **D1 severity 大幅下降** —— latch 不再是「判据不可满足」的原因，它剩下的真问题是 E2「④′ 测的是罩不是策略」= **可解释性**问题而非**不可满足**问题；② **R-2「逼障带动力学不可学」大部分消解** —— `[3.5, 5.0]` 落在罩的**非介入区** ⇒ 该区动力学**语料里本来就有**；③ **P5（trigger 3.0→2.0）两个理由全失效** ⇒ 很可能**完全不必要**，若确认则项 6 记「撤案」；④ **①′d-b 与 ④ 的对抗性消失** —— 新带与 ④ 冻结的 `1.5 m` 不再争同一区间；⑤ **可证明的「逼障」含义收窄**为「贴着 standoff 边缘紧绕行」，飞进 `[1.5, 3.0]` **被部署罩结构性禁止** ⇒ **原 §1.3.2 的框架与部署构型本就不自洽**，此点登记而非绕过。

  另五条就地更正（**全部是收紧或补未定义分支，无一放松**，= 签字项 5y）：**G3** 我自己的 F1 修法不完整 —— 分子改 27 后，直飞满 30 m 的轨迹得 27/30 = **0.90 = 恰好等于阈值**（完美策略卡刀口）⇒ 路径长必须在**首次到达步 `T_arr` 截断**，直飞 = 1.0；**G4** 盲对照臂用**置零图**是 **OOD**（深度头与 WM 都没见过全零）⇒ 两层齐挂但挂因是分布外崩溃、非「缺视觉」⇒ 改**时序打乱/错配帧**，并加第三分支「两层都 FAIL ⇒ **inconclusive**，不得当作视觉依赖的证据」；**G5** E3 的固定公共子集 `P` 有**未定义分支** —— `s ∈ P` 但 actor 未到达时效率/带占用无定义（撞墙短路径 ⇒ 虚高），剔除又回到非单调病 ⇒ **填 0** 计入 median，并要求 **`|P| ≥ 8`** 否则 `authoritative=false`；**G6** ④′c 的「`K_off<5` ⇒ 换起点集重跑」**无界** ⇒ 改**有条件**：仅当 **①′a-b PASS** 时才重采；①′a-b FAIL ⇒ gate 已由 ①′ 判 FAIL，不重跑（否则可无限循环并掩盖真正的 ①′ 失败）；**G7** v5 的 `release_depth_m=4.0` **不可当常数** —— 晚⁷ 后 D̂ 在近带系统性欠读 ⇒ 解锁可能**永不触发** ⇒ **v5 静默退化成 v4** ⇒ 该值须由 P3(⓪) 的 D̂↔GT 偏差定：`min{r : P(D̂ ≥ r | GT ≥ 4.0) ≥ 0.9}`（这是 **P3.5 必须排在 P3 之后的第二条理由**）。

  门禁未动：§4.1 阈值（`1.5 m` / `0.80`）、`enable_policy_update=false` 全未改；R1 on 125 不受影响。**头号阻塞签字项：5r′ → 5x。**

- **2026-08-18（判据提案整合：新增 §4.6「判据 v2 整合稿」= 实施唯一权威口径；并**更正第四轮自己对 D1 的推荐**）** — 改了什么：提案新增 **§4.6**（4.6.0 shield latch 头号裁定 / 4.6.1 通用要求 v2 / 4.6.2 ⓪ v2 / 4.6.3 ①′ v2 / 4.6.4 ④′ v2 / 4.6.5 ⓿ v2 / 4.6.6 前提链 v2 / 4.6.7 溯源表），签字栏 **5r 划掉改 5r′**、新增 **13–16**，头部加「实施口径」警示。**§1 / §2 原表一律保留不改写**（审计链）。**代码 / yaml / 阈值仍一行未动。** 为什么：§4.4 + §4.5 共 36 项并列注记里，D1–D4 与 §1 原文**直接冲突**（§1.0 写「①′ 在 shield-on 下测」，D1 证明那样测合取不可满足）⇒ 再往下走会有人照 §1 实施 ⇒ 必须有一份整合口径。依据 / 关键更正：**第四轮给 D1 的推荐候选 (i)「非 latch 瞬时罩」被 `safety.py:48-79` 的四代设计史推翻** —— 那正是历史设计 **(1) pre-latch hover**：目标寻径持续前推、罩持续抵消 ⇒ **在带内外振荡 ⇒ `near_coll_rate_on ≫ off` ⇒ ratio 反转 ⇒ ④′b 直接 FAIL**；第四轮只读了 `should_override`/`override_action` 两个函数体，**没把 docstring 的设计史读进裁定**，此处更正。四代史：(1) 非 latch+hover ⇒ 振荡/ratio 反转；(2) latch+无界后退 ⇒ 盲退倒撞后墙（晚¹⁰ `coll_after_latch=9/9`、latch 后 ~33 步撞）；(3) latch+纯 hold ⇒ 零 delta 不消前向动量、滑入带驻留（晚¹¹ `near_count_on` 200/200、`rate_on` 0.385、**ratio 12.96**）；(4) latch+有界状态反馈后退（现行）⇒ 三病都修，代价 = 策略整局失效 = D1。⇒ **新推荐裁定 = 第 (5) 代：有界状态反馈后退 + 滞回解锁** —— 触发不变（`D̂<3.0` ∪ `τ<1.0` ∪ `p_coll>0.5`），latch 期行为不变，**解锁** = `D̂ ≥ release_depth_m(提议 4.0 > 触发 3.0，有滞回带)` **连续 M=5 步**（5 Hz 下 1 s）后交还控制权，**每局解锁 ≤3 次**、超限则本局永久 latch；逐局落 `latch_step[]/release_step[]/n_release/frac_steps_engaged`。第 (1) 代的病根是**触发面与解锁面重合、没有滞回**，分离两面 + 连续确认把振荡频率上限压到 `1/(M·dt)`，次数上限再给 `rate_on` 污染一个硬上界。⇒ 策略退到 4 m 稳定 1 s 后**重新获得控制权可以转向绕行**（罩本身永不转向，E2）⇒ ①′a-b 与 ①′d-b 不再互斥，D1 解。**强制前置 = 新增 `P3.5` shield v5 回归验证**（负载 = shield-on 的 **heuristic 探针**，非被测对象 ⇒ 不构成「为过 gate 调罩」）：不振荡 `ratio ≤0.80`、不倒撞 `coll_after_latch=0`、不驻带 `rate_on ≤0.05` 且 `frac_in_band ≤0.05`、解锁真发生（`n_release ≥1` 的局占比 ≥0.5，否则 v5 退化成 v4）；**P3.5 必须在 P3(⓪) 之后** —— v5 的解锁条件**信任 `D̂ ≥ release_depth`**，而那正是 ⓪d 要保证的东西。fallback 顺序 = (ii) 拆能力臂/安全臂 → (iii) 只判未 latch 局（不推荐）；**(i) 退出候选**。红线关系：latch 是**控制律**（红线明文可改）⇒ 改 v5 合法；`1.5 m`/`0.80` 不动 ⇒ 非「调阈值凑过」；但 v5 改**部署构型** ⇒ 触发 E6 ⇒ **必须同时 re-freeze V1 部署构型并声明 V4 ④ 与 V1 历史 ④ 不可比**（C9 的 V1 对照臂正式移除，登记为已知代价；④′a 本已是绝对 0，不依赖该臂）。§4.6 其余 v2 改动（全部标注来源与「收紧 / 口径修正」性质，见 §4.6.7 溯源表）：**收紧** —— `path_efficiency` 分子改 `‖g−p₀‖ − arrival_radius`（F1：旧上界 1.11 ⇒ 0.90 实际允许 23%，改后回到真 10%）、⓪ 加 **⓪c p90 AbsRel ≤0.50** + **⓪d 功能项 `P(D̂ > trigger | D_gt ≤ trigger) ≤ 0.05` 且不得 ≥2 连续漏帧**（ε 与「≤1 孤立漏帧」由 5 Hz×1 m/步 ⇒ 3 m 有 3 步制动余量推出）+ ⓪b support 改逐帧（`n_frames ≥100`、单帧占比 ≤0.2）、新增**盲对照臂**（F7/B1，必须在 S_blocked FAIL，否则 gate 与视觉无关）、新增 **primary/secondary 划分 + 每条 OC 曲线 + seed 裁决规则跑前冻结**（R-13 合取误 FAIL ≈54%）、新增**起点几何独立性**（两两最小间距 ≥10 m 提议，否则 B3 功效计算无效）；**口径修正** —— 起点池改 `candidate_positions` 且只打标签不准入（D3+洞 3）、band 改**全向最小净空**（离线位姿回放、≥4 方位 body ±x/±y 取 min，`center_frac` 退出）（D4+A4+C8）、①′c-b/①′d-b 的 median 改在**固定公共子集 = P7 planner 到达的 start** 上报（E3，恢复对策略质量的单调性）、①′b-b 由恒真自检改为**分层器确定性自检**（同起点重复探针 R≥3 含变 seed，一致率=1.0，不成立 ⇒ gate 作废而非 actor FAIL）（F4+C5）、band 分母排除**罩接管步**（F3）、④′c 改**遭遇机会 + N/A 处置**（`K_off ≥5` 且 `rate_off==0` ⇒ N/A 不入 merge；`K_off<5` ⇒ vacuous 且须换起点集重跑）（D2）、⓿ 写死**真实侧 G = analytic**（E5）+ z0 重置可行性先实测（C4）、GT 深度**只离线取**（A2）、`step_hz` 重测冻结（F5）、冻结清单 +`cruise_alt_m`/`goal_dist_m`/起点池来源/v5 参数（F6+B4）、到达改 `min_t dist ≤3.0` 或到达即终止（B5）、S_open 加 **shield-on heuristic** 经验上界兼罩过敏判别（E7）、④′b 加两臂步数/命中数直方图前提（E1）、登记 shield-off 的「策略自身安全」诊断（E2）。**前提链 v2** = P0 → P1 → P2 → P3(⓪ v2) → **P3.5(v5 回归)** → P4(⓿ v2) → **P4.5(语料重采+WM 重训 ⇒ 重跑 P1/P4)** → P5(暂缓，且 **v5 解锁已部分替代 P5 对 ①′d-b 的作用 ⇒ P5 可能不再必要**，实测后裁) → P6 → P7(加「E3 公共子集来源」+ v5 第二次回归负载) → P8。仍作为**已知残余风险**不解的：R-1（前飞 vs 斜绕差 8%）、R-3（`p_coll` 死）、R-16（无预注册停止规则）、「主动终止」零判据。门禁未动：§4.1 阈值、`enable_policy_update=false` 全未改；R1 on 125 不受影响。
- **2026-08-18（判据提案第四轮复核，读码确证 → 补 18 处，其中 4 处让判据在当前实现下「不可满足 / 不可跑」）** — 改了什么：提案新增 **§4.5 复核补漏（D 组 4 致命 / E 组 7 重要 / F 组 7 应补）**，§5 签字栏加 **5r–5w**，第 6 项（P5）加「须在 5r 之后」。**代码 / yaml / 阈值仍一行未动。** 为什么：第三轮是纸面推演，**没读 `safety.py` 与 `v0_metrics.py` 的实现**就写了 §1.0「①′ 在 shield-on 下测」与 §1.3.2 带占用；本轮读码，发现新判据与既有实现**互斥**。依据（全部读码 + 手算，无渲染无训练）：**D1 shield 一旦触发就 latch 整局** —— `safety.py:115-116` `if self._engaged: return True`，`reset()` 只在**局间**清（`v0_rollout_eval.py:912`），触发后 `override_action`（`:137-140`）= 「D̂<3.0 就 body −x 后退、D̂≥3.0 就 hold」⇒ **策略在本局剩余全部步失效**、被钉在 standoff 直到 `max_steps` ⇒ 在 shield-on 下 S_blocked 里**任何贴到 3.0 m 的策略本局当场结束** ⇒ `arrival` 不可能 ⇒ **①′a-b ≥0.50 与 ①′d-b ≥0.10 互斥、§1.3.3 合取不可满足**；并**推翻 §1.5 第一行**（「①′ vs ④′ 交集只有逼障」—— latch 罩下**交集是空集**）。修 = 三选一预注册（非 latch 瞬时罩 / 拆能力臂(off)+安全臂(on) / 只判未 latch 局[不推荐，会变成隐式起点筛选]），`latch_step` 逐局落盘；**P5 由「优化项」升级为 ①′d-b 可满足性的必要条件，但去不掉 latch 仍不可满足**（签字项 5r，**阻塞项**）。**D2 ④ 的冻结实现里 `rate_off == 0` 是 FAIL 不是 N/A** —— `v0_metrics.py:305-307` `if ... rate_off <= 0: ratio_ok = False`、`:313` `overall_ok = (before_ok is not False) and ratio_ok` ⇒ 理想逼障策略（停在 [1.5,3.0]、**从不进 1.5 m**）off 臂 near-coll = 0 ⇒ **④ 直接 FAIL** ⇒ **第三轮 A1 记的「被判 vacuous」低估了严重性**；且我提的 ④′c（`near_coll_rate_off > 0`）在冻结码里**已等价存在** ⇒ A1 的修法**不能只写在 ④′c**，必须出**显式方法学 re-freeze 注记**（与洞 2 对 ④b 的裁定同型）：满足遭遇机会 K 时 `rate_off==0` ⇒ **N/A 不入 merge**；`0.80` 与 `1.5 m` 仍**一字不动**（改的是空样本处置，不是阈值）（5s）。**D3 §1.3.0 用 `make_start_episodes` 会让 S_blocked 填不满** —— 该函数**所有局共用同一起点** `[0,0,cruise_alt]`（`v0_rollout_eval.py:233`）只随机 heading，而 `env_airsim_16` 在巡航高度**基本开阔**（`:269-271` 原注释：「a level goal over the origin meets nothing → `near_coll_rate_off==0` → ④ vacuously fails」）⇒ (a) 扫不出 16 个 S_blocked ⇒ **gate 不可跑**；(b) 32 局是**同一点出发的 32 条射线**、几何高度相关 ⇒ **第三轮 B3 的二项功效计算（独立性前提）被破**，有效样本远小于 16；(c) 无 `start_clearance_m` 检查。修 = 起点池**保留 `candidate_positions`**（真实轨迹位置 + teleport 询问，是唯一能找到障碍的机制）但**去掉深度窗与 `probe_near_m` 准入**、只由探针**打标签** —— 既保住「找得到障碍」又消掉洞 3 选择偏差；落盘起点两两最小间距与 `cruise_alt_m`（5t）。**D4 ①′d-b 带占用用「前向中心裁剪」对理想行为不可满足** —— 真正的贴障绕行是**侧掠**（障碍在侧、前向开阔）⇒ `frac_steps_in_band ≈ 0`；唯一能刷到带占用的姿态是**把机头对准 3 m 内的障碍**，而那恰好触发 shield = **D1 的整局失效** ⇒ 要么测不到目标行为、要么测到就自毁；与第三轮 A4（yaw 解耦）**同源但更强**（换成速度方向也不够）。修 = 带占用改**全向最小净空**（离线位姿回放 + 场景几何 / 全场深度 min），`center_frac` 退出该判据（5u）。**E 组七项**（5v）：**E1** ④′b 的 ratio 是 **pooled per-step**（`v0_metrics.py:296-301` `hits/总步数`，跨局池化）⇒ latch 后 on 臂被拖到 `max_steps` 的安全步**稀释** `rate_on`、off 臂早撞局短 ⇒ **ratio→0 结构性通过**，且 `max_steps` 越长越宽（B4 未冻结）⇒ 须落盘两臂总步数/命中数/每局步数直方图并预注册「ratio 只在步数量级可比时可解释」（0.80 不动）；**E2** 「让 shield 替我避障」这条空过路线**读码排除** —— `override_action` 只有 body −x 后退与 hold（`:139-140`）、**不转向** ⇒ 罩不能替策略绕行，但反面是 ④′a/④′b 的高分**主要由 latch 产生不由策略产生** ⇒ **④′ 现口径测的是罩不是策略**，须登记一条 shield-off 的「策略自身安全」诊断（不入 merge）；**E3** ①′b/c/d 的 median 只在「到达 ep」上算 ⇒ **判据对策略质量非单调**（到达集合由被测策略自己决定：只在最容易 8 局到达的差策略在**容易子集**上算效率，能到 14 局的好策略在**更难的局**上算 ⇒ **把策略改好可能让 ①′c-b 从 PASS 翻成 FAIL**，等于奖励「保守放弃难局」）⇒ 效率/带占用改在**固定公共子集**（建议 = P7 planner 到达的 start）上报或按 start 配对，到达率仍全 16 局；**E4** ⓪ 的 `AbsRel ≤0.30` **不是它自称要保证的功能** —— AbsRel 0.30 在 `D_gt=3.0 m` 允许 `D̂` 高读到 **3.9 m** ⇒ **罩不触发**而 ⓪ 仍 PASS，median 还允许**一半像素任意坏** ⇒ 增补功能项 `P(D̂ > trigger | D_gt ≤ trigger) ≤ ε` + 用 **p90** 而非 median + support 改**逐帧/逐局**（现 ≥1e4 px 可以全来自一帧）；**E5** ⓿ 没写**真实侧的 G 用哪个 reward**（真实侧 analytic + 想象侧 RH ⇒ 混了「RH 诚实」与「动力学保真」；两侧都 RH ⇒ 「系统性错但单调」的 RH 可通过）⇒ 写死真实侧 G = **analytic 纯位置几何**，并声明 R1 后 ⓿ 只测动力学；**E6** **P5 与红线「训练/评测 shield 路径与 V1 部署一致」冲突未裁** —— trigger 收到 2.0 重采则训练侧罩 ≠ V1 部署罩(3.0) ⇒ 要么同时 re-freeze V1 部署构型、要么声明与 V1 历史 ④ 数字**不可比**（则 C9「相对 V1 不回归」随之失效）；**E7** 探针 shield-**off** 而 actor shield-**on** ⇒ 分层标签与被测构型不同，S_open 的效率上界是 off 臂量的、on 臂侧向障碍可能误触发**且 latch ⇒ 直接飞不到** ⇒ ①′a-o/①′b-o 可能因**罩**而非策略 FAIL ⇒ S_open 上**再跑一次 shield-on heuristic** 作为经验上界与罩过敏判别（C2 只登记了 `intervention_rate`，没把它变成阈值有效性前提）。**F 组七项**（5w）：**F1** `path_efficiency` 在**到达局也系统性 >1**（到达半径 3.0 ⇒ 最短行程 27 m、分子 30 ⇒ 上界 **1.11**）⇒ 0.90 实际允许 **23%** 冗余 ⇒ 分子改 `‖g−p₀‖ − arrival_radius` 或按 1.11 归一后重述口径（**口径对齐不是放松**，必须跑前定）；**F2** 效率**只算平移** ⇒ 原地转 yaw 不增路径长、「磨蹭旋转」只被未冻结的 `max_steps` 罚 ⇒ 落盘累计 yaw 转角；**F3** ①′d-b 分母**必须排除 latch 后的步**（否则 hold 步稀释占比）；**F4** ①′b-b 是**同一次探针跑出的标签的自检 ⇒ 恒真零信息**，要有信息必须**换 seed 重跑探针**（与 C5 合并）；**F5** ①′ 改 shield-on ⇒ 每步多跑 D̂/τ/p_coll ⇒ **`step_hz` 必须重测并冻结**（红线「实测不猜」；注：D̂ 是网络前向，**不是**渲染器 GT 请求 ⇒ 与 A2 的 ~3 Hz 是两件事）；**F6** 冻结清单缺 **`cruise_alt_m` / `goal_dist_m=30` / 候选起点池来源**（S_open:S_blocked 比例**完全由这三项决定**）⇒ 与 `max_steps`、到达半径同级冻结；**F7** **S_open 由「特权盲策略」构造性通过**（heuristic 本身满分）⇒ S_open 三项**不构成任何视觉能力证据**、只是退化护栏 ⇒ 加**盲对照臂**（图像置零/打乱、其余不变）**必须在 S_blocked FAIL**，若它也过则 gate 与视觉无关 —— 这条便宜且**直接补上 B1 的反事实缺口**。门禁未动：§4.1 阈值、`enable_policy_update=false`、shield/τ/depth 路径全未改；R1 不受影响。
- **2026-08-18（判据提案第三轮复核 → 补 18 处遗漏，其中 4 处会让判据自毁/不可执行）** — 改了什么：提案新增 **§4.4 复核补漏**（A 组 4 项致命 / B 组 5 项重要 / C 组 9 项应补），§5 签字栏加 **5h–5q**。**代码 / yaml / 阈值仍一行未动。** 为什么：用户要求再复核一遍找遗漏 —— 且 A1 是**本稿 §1.4 自身的 mis-specification**（我今天写的反空过补丁踩了与旧 ④ 对称的坑），必须自查出来。全部读码 + 手算，无渲染无训练。依据：**A1 ④′c 会把「理想目标行为」判成 `vacuous`** —— 逼障的定义就是停在 [1.5,3.0] m **不进 1.5 m**，而 ④′c 要求 `near_coll_rate_off > 0` ⇒ **完美达成本稿目标的策略 off 臂 near-coll = 0 ⇒ 不得记 PASS**，且 §1.3.3 merge **未定义 N/A 怎么并** ⇒ 旧 ④ 把「从不接近」当安全，④′c 把「从不危险」当空过，**同型对称陷阱**；修 = 反空过改**遭遇机会**口径（`n_steps_forward_depth<3.0m (off) ≥ K`，建议 K=5），横漂仍被挡而称职策略不再被罚（签字项 5h；本稿未签 ⇒ 合法修法是并列注记，§1.4 原表**不改写**）。**A2 逐步 GT 深度落盘会改变闭环速率** —— 已确立跨网闭环 RGB ~14 Hz / **+depth ~3 Hz**、部署 `step_hz=5`，而 ①′d-b + ⓪ + 撞点 7 项全需逐步 GT 深度 ⇒ 在线取就把 dt 改了 ⇒ 违反「训练/评测 shield/τ/depth 与 V1 部署一致」且是 rate-lock 已治好的 dt-desync 复发路径；修 = **GT 深度只由位姿回放离线取**，在线只落 pose/D̂/action/intervention（5i）。**A3 P0–P8 顺序错** —— §3.1 完整重采 + §3 goal ≥32 ⇒ **WM 必须重训** ⇒ P1/P3/P4 都要在新 WM 上重做，而现链条是 P1 先给旧 WM 发证、随后 P5→重采→WM 换掉、**证书作废**；修 = 插入 **P4.5 = 语料重采 + WM 重训**（5j）。**A4 yaw 解耦攻击**（§1.3.5 空过审查漏项）—— 带占用用「前向中心裁剪」、shield 锥按 body-forward ⇒ 策略可**转开机头、侧向平移**，带占用测不到贴障、shield 前向锥同时瞎、§1.3.7 撞点归因第二行会误判；且 heuristic `yaw`≡0 ⇒ 探针对 yaw 无参照；修 = 带占用与撞点归因按**速度方向**算 + 落盘 `angle(yaw_forward, velocity)`（5k）。**B1 反事实臂被本稿自己删掉** —— 旧 ① 至少有 heuristic 比较臂，新口径全绝对阈值 ⇒ ①′④′ 全过只证「有一个策略能干活」，**不证「想象 AC 必要」**（而这才是设计 §2.4 的主张）；修 = `actor ≥ P7 planner 到达率` 预注册为增益判据，**或明确宣告 MVP 不主张，不得沉默**（5l）。**B2** 训练起点集与 gate 起点集未要求互斥 ⇒ 可能在测记忆（5m）。**B3 功效从未计算**（本稿批评 V1-① 功效脆弱却没算自己的）—— n=16 手算：①′a-o ≥0.875（需 ≥14/16）对真值 0.95 通过 **95.7%**、但对真值 **0.80 仍 35.2%** 放过；①′a-b ≥0.50（需 ≥8/16）在真值 0.50 处 **59.8%**（边界抛硬币）、真值 0.35 处 15.9%；**合取方向更要紧** —— 15 条子判据全 AND，每条对好策略 5% 误杀 ⇒ **merge 误 FAIL ≈ 1−0.95¹⁵ ≈ 54%**，而红线禁事后放松 ⇒ **跑出来就卡死**；修 = 跑前落盘每条 OC + 预注册 seed 数与「两 seed 不一致」裁决（历史两 seed 给 n=7/8、`rejections` 708 vs 15）+ 声明 **primary vs secondary**（5n）。**B4** `max_steps`/时间预算未冻结，而到达率与效率都依赖它（5o）。**B5** `final_dist ≤ 3.0` 是末态 ⇒ 若无到达终止，**冲过目标再飞走记未到达**（想象里已见第 ~8.4 步冲过 30 m）⇒ 改 `min_t dist ≤ 3.0` 或到达即终止；另最终目标含「**主动终止**」而本稿**一条没测** ⇒ 加判据或明确 out of scope（5p）。**C 组九项**（5q）：C1 ①′d-b 用 GT 深度**只测结果不测机制**（瞎子也能因场景几何落带里）⇒ 机制主张全压 ⓿ 与 P7、须落盘进带前 D̂ 与减速/侧移；C2 **S_open 干预率**没落盘 ⇒ ①′a-o FAIL 可能是**罩过敏**；C3 ⓪ 须在**部署分布**（S_blocked 评测帧）上测而非只深度 holdout；C4 ⓿ 的 z0 若取自被测 actor 就不是前置门，且 K×z0 需**精确状态重置**、AirSim harness 是否支持**没查** ⇒ 不支持则 ⓿ 现写法不可实现；C5 **探针确定性未验**（标签是全部判据基石）；C6 落盘契约进 `--self-check`，缺字段即 `authoritative=false`（治 partial-1 缺 `collided` 复发）；C7 **无预注册否证/停止规则** —— P7 无参数 planner 都过不了 ⇒ V4-MVP 前提被否证、应停训 actor 回到 WM/感知，现方案是**没有下车站的梯子**；C8 `frac_steps_in_band` 分母与 `center_frac` 未定义；C9 ④′ 移除 V1 对照臂引用**仅登记**。门禁未动：§4.1 阈值、`enable_policy_update=false`、shield/τ/depth 路径全未改；R1 不受影响。
- **2026-08-18（判据提案同日第二次修订 → 退化行为抑制机制说清；查明「逼障在当前语料下不可学」）** — 改了什么：提案新增 **§1.4b**（退化行为：检测侧三行表 + 目标函数手算表 + 两个真弱点 + 「不动」的训练期护栏）与 **§1.4c**（语料记的是执行后动作），§5 签字栏加 **5e/5f/5g**；`LIVING_DOCS` §D 加 3 条防误读。**代码 / yaml / 阈值仍一行未动。** 为什么：被问「这个方案怎么避免学出完全垂直 / 绕飞 / 干脆不飞」——判据只管「不让退化混过 gate」，actor 优化的是**想象里的 RH 回报**，两件事必须分开写，否则文档会被读成「判据已经解决了退化」。依据（读码 + 手算，无渲染无训练）：**(1) 检测侧三种都被合取挡住**：横漂 ⇒ 两层 ①′a 全 FAIL + ④′c 反空过标 `vacuous` 不得记 PASS；绕飞 ⇒ ①′b-o 效率 <0.90（专为它设）+ ①′c-b/①′d-b；不飞 ⇒ 两层到达率 0 + ④′c `vacuous`。顺带：**旧判据排序完全反的** —— 撞墙 ~+9 > 不动 0 > 横漂 −7.43，而 ④ 给不动/横漂**最高安全分**。**(2) 真实几何下退化已被罚**（`w_progress=1.0`/`w_maneuver=0.01`/H=15/5 Hz 盒 `[1.0,0.4,0.4,0.314]`/‖g‖₀=30，未折扣，γ¹⁵≈0.956 不改排序）：前飞 **+14.85**（Δ‖g‖ +15.0、maneuver −0.15）> 斜绕 **+13.68**（+13.84/−0.16）> 不动 **0.00** > 纯横漂 **−0.65**（终点 ‖g‖=√936=30.594 ⇒ Δ −0.59、maneuver −0.06）⇒ 不动与横漂**不是局部最优、是被罚的** ⇒ 实测 85° 横漂**不由奖励结构解释** ⇒ **抑制退化的主力是 P0（R1），不是判据**（analytic progress 是几何真值，谎报通道被结构性关闭）。**(3) 两个真弱点，不掩盖**：(i) 前飞 vs 斜绕只差 **8%**，而 RH 误差 4–6× 足以翻转（已实测翻转：几何 15.0>12.2、RH 排成 81.64>62.60），且 H=15×1 m 只覆盖 30 m 的一半 ⇒ **绕大圈的代价落在想象窗口外**；本周期**无干净训练侧抑制**（per-step 时间成本能罚绕行但同时压制「贴障需减速」、与逼障冲突；「沿最短绕行路径的进展」需特权路径 oracle ⇒ 属 V2 拓扑层，越界）⇒ 登记为**已知残余风险**（签字项 5g），只检测、不假称已解。(ii) `p_coll` 死（≈6e-4）⇒ 想象里撞墙零成本 ⇒ 短视目标实际偏好**直冲穿墙**（正是 +14.85 那条臂）⇒ **P2 不只是 ④ 的前提，也是「不学成直冲」的前提**；这同时反证横漂既非目标函数偏好、也非 p_coll 逼出。**(4) 「不动」有判据覆盖不到的风险**：C2 是 `a = limits ⊙ tanh(u)`、mean≈0 ⇒ **`a=0` 正是初始化点**，RH/critic 信号弱时策略停在初始附近 ≈ 不动，而 gate 只在**训完后**测 ⇒ 训练期须落盘 imagined ΣG / analytic ΣΔ‖g‖ 的**比值**（= R1 臂 (b) 的量，签字项 5f）。**(5) 语料记的是执行后动作（新查明）**：`collector.py:186` 用 `self.safety.override_action(obs)` **重绑** `action`，`:196` 存进 `Transition` 的就是这个覆盖后动作，指令动作**被丢弃**（只剩 `ep_info["intervention"]` 布尔，`:191`）。好的一面：WM 学 `(a_executed → z')` = **真实动力学**，**没有**把 shield 学进转移函数（两个分支里较好的那个）。坏的一面：语料里**不存在**「距障 <3.0 m 且指令前飞」的 `(z,a)` 对 ⇒ `dynamics.step` 在 **[1.5,3.0] m 带上是纯外推** ⇒ **想象里「贴障前飞」的后果是虚构的** ⇒ **§3.1 从「语料偏斜」升级为「逼障不可学」**（不是正例少，是该区域动力学从未被观测）；合法解仍只有 P5 那条（收紧触发让罩更晚介入、罩始终在），**禁止关罩造正例**，且 P5 方向须等撞点落盘裁定。附带落盘缺口：指令动作被丢弃 ⇒ 无法从语料测「策略在障碍前想往哪走」，§0.2 活假设只能测到 `intervention` 占比这一层 ⇒ 加 `action_commanded` 字段（签字项 5e，只加字段不改控制流）。门禁未动：§4.1 阈值、`enable_policy_update=false`、shield/τ/depth 路径全未改；R1 不受影响。
- **2026-08-18（判据提案同日修订 → 补两处漏项：无效局 / 可解性证书；P5 方向改暂缓）** — 改了什么：提案 §1.3.0 加第 4 点（**无效局**）、新增 §1.3.0b（**可解性证书**）、§1.3.6（**碰撞局记账**含「起飞很早就撞」）、§1.3.7（**撞点必须落盘的 7 项**）、§2 P5 加「方向可能相反」注、§5 签字栏加 5b/5c/5d 且第 6 项改 **暂缓**。**代码 / yaml / 阈值仍一行未动。** 为什么：被问「起飞很早就碰撞算什么」时发现原稿两处自毁 —— **(1)** 未区分 `steps_to_collision==0`（spawn 即碰撞）与真早撞，前者若记成 `hard_coll` 则 **④′a = 0 永远不可能达成**，判据自毁；且它与已知「① n=5 丢局在评测期」**同源**（`_run_one_resilient→None`），修丢局时须把 `n_invalid_spawn` / `n_none_returned` / `n_pair_broken` **分开**落盘。**(2)** S_blocked 未做可解性区分：`start_clearance_m=3.0` 只保证起点 3 m 净空、**不保证存在绕行解**，探针 2 m 就撞与 22 m 才撞混在一个 median 里会污染 ①′a-b 分母 ⇒ 按 `d_first_contact < 5 m` 拆 **S_blocked_tight** 单独报告，并以 **P7（planner 纯前向）到达率作为经验可解性上界**（planner 也过不去 ⇒ 该起点可解性「未知」，不得据此判 actor FAIL）；**禁止**事后按「太难」剔除已评分 start（选择偏差，洞 3 同类）。**(3)** 顺带堵住一个与旧 ① 同型的错误：`path_efficiency = ‖g−p₀‖/Σ‖Δp‖` 在撞墙短路径上会**虚高（可 >1）**⇒ 效率与带占用**只在到达 ep 上算**，否则早撞反而抬分。**(4)** P5 方向可能反：若撞点落盘显示「shield 已触发仍撞」，病是**反应余量不足**，触发深度应保持 3.0 或加大，而非 3.0→2.0 ⇒ **P5 必须在 P3（⓪ 近带深度）与首轮撞点落盘之后裁定**。先验是余量够（5 Hz × 前向上限 1.0 m/步 ⇒ 3.0 m 有 **3 步**制动余量 ⇒ 第一嫌疑是**深度**，与 V0 ④ 根因「1.5 m 墙读成 ~6.4 m」同型），但**实测优先，不猜**。门禁未动：§4.1 阈值、`enable_policy_update=false`、shield/τ/depth 路径全未改；R1 不受影响。
- **2026-08-18（判据本身立案 → 目标改「逼障」、口径改「分层到达」；新增两条前置门）** — 改了什么：**新增** [`V4_CRITERIA_REFREEZE_PROPOSAL_20260818.md`](V4_CRITERIA_REFREEZE_PROPOSAL_20260818.md)（待签字）；本文新增 **§1.1**；`LIVING_DOCS` A 表加 2g、§D 加 6 条防误读、§E 加 2g；`PROJECT_STATUS` §2/§6 同步。**代码 / yaml / 阈值一行未动。** 为什么：五轮 FAIL（M5→M5e）的全部归因都预设「①④ 是对的判据」，08-18 发现该前提不成立 —— 判据错配是诊断链上**最后**才被怀疑的对象（怀疑它要 re-freeze，成本最高），加上 ④ 四轮全 PASS ⇒ **PASS 的信号从未被复核**。依据（全部读码 + 已落盘数，无渲染无训练）：**(1) 旧 ① 奖励撞墙** —— `progress_sum` 从位置算（`v0_rollout_eval.py:584-632`，确实不被 RH 污染）但 `p_final = ep[-1].next_obs.position` 且 collector 遇 `done` **break** ⇒ 直线撞墙 = 停在离目标最近点 = progress 拿满（heuristic 每局 ~8.7 的来源），而正确绕行的横向位移**不计分**；且 `HeuristicPolicy`（`train_rl.py:33-74`）是**特权 proprio 直线 oracle**、`yaw` delta ≡ 0、**从不避障**，同时又被当作起点筛选探针（`v4_gate_run_partials.py:125-139`）—— 这层构造性事实在 V4 全部文档里**从未记载**。**(2) 旧 ④ 可被横漂空过** —— on-rate 0 + `n_contact=0` ⇒ ④b `before_vacuous` + ④c **0.000** ⇒ 自动 PASS；即 M5c/M5d/M5e 的「④ PASS」与「① FAIL」**是同一个病的两面**（横漂 `cos_path_goal` +0.075/+0.098 ≈85°），⇒ **即使 ①④ 双 PASS 也不能证明想象 AC 有用**。**(3) ①④ 互为对抗** —— 直线撞墙 ①高④差 / 远离绕行 ①差④好 / **逼障 ①高④好（唯一双赢）**；merge 的 AND 假设「独立约束」不成立。**(4) 逼障被结构性禁止** —— shield 触发 **3.0 m**（控制律）vs ④c 度量带 **1.5 m**（§4.1 阈值）⇒ 可行包线仅 1.5 m 窄带，且训练语料 shield-ON ⇒ 该带**不存在**，策略只学到「靠近 ⇒ 动作被改写」（横漂 85° 的候选成因；① eval 本身 shield-off，故此假设只解释训练侧，验法 = 统计语料 `intervention` 占比与其前向 GT 深度分布）。**(5) 近带深度从未 gate** —— ①d 的 0.0641 是 `D_gt ∈ (0,200]` **全域 median**，与「1.5 m 墙读成 ~6 m」可共存（V0 ④ 根因即此）。**(6) V4 用的 WM 从未过 V1-②** —— V1-② 确是多步保真（`reward_beat_frac` 0.93）但 bar 是「打赢常数基线」不是校准（高估 4–6× 也能赢；校准曲线 t0–t4 才准、t≈6 起爆）、当时 `coll_ok=null`（`coll_claimed=false` ⇒ p_coll 保真从未主张）、且 V4 实际用 `wm_ckpt_r60_rh_20260816/wm_step_1000.pt`，**换 WM 没重跑 ②**。**(7) 「只留直线到不了的起点」是另一个极端** —— 开阔路径上直线**就是**正确解，只判遮挡层会训出「永远绕圈」。⇒ 新口径 = **分层到达**：探针打标签成 S_open（判不退化：到达 ≥0.875 / 效率 ≥0.90 / 硬碰 0）与 S_blocked（判逼障到达：到达 ≥0.50 / 效率 ≥0.70 / [1.5,3.0] m 带占用 ≥0.10），**merge 取合取**；新增前置门 **V4-⓪**（`D_gt ∈ (0,3.0]` median AbsRel ≤ 0.30，**阈值不降**）与 **V4-⓿**（想象排序一致性 Spearman ρ ≥ 0.50 + top-1 ≥ 0.50 —— 设计 §1.3 把「多步预测误差」列为成功指标，但 V0/V1/V4 信号表里**从无一条测它**；⓿ 同时是 `planner.enable` 的可用性判据）。**这不是降阈值凑过**：§4.1 阈值一字不动（④c 0.80 / 1.5 m 带 / ①d 0.30 / ② +5.0−3.0 / ③ 0.25），新表在两层上都比旧表严（旧表撞墙即可达成 +10%），`n` 从 8 提到**每层 16**（收紧），另加两道门；`enable_policy_update` 保持 **false**；旧 §4 ①/④ 两行**原文保留不改写**（审计链纪律），旧 progress 降级为诊断。流程补丁 P1–P4（每条 PASS 判据必附「什么坏策略能让它 PASS」/ merge 前做信号两两对抗检查 / 退化 PASS 标「需解释」/ 冻结判据前先实测其输入分布）—— 若 08-16 已执行，(2)(3) 当场暴露、洞 4 提前两天。与正在跑的 **R1 正交，不打断**（R1 = 提案 §2 的 P0）。**提出方 = 复核方**警示已写在稿首：§1.3.5 的「找不到能空过的坏策略」须由**独立一轮**复核后再签。
- **2026-08-16** — 规格落地；125 agent 接手 M0–M4。
- **2026-08-16 晚** — M1–M4 代码入库；`_v4_gate --self-check` PASS；`v4_ac_smoke` OK。
- **2026-08-16(M3)** — 125→H100 SSH key; H100 `train_v4_ac` 10 iters PASS (stub).
- **2026-08-16(M5)** — 125 4090 rollout (stub encode): ① FAIL / ④ PASS; yaml 未翻。
- **2026-08-16(encode-train)** — Align train+deploy to torch WM encode; H100 300 iters; re-gate `v4_gate_r60_20260816_wm`: ① FAIL, ④ FAIL; merge FAIL; yaml 未翻.
- **2026-08-16(reward-head)** — M5c done: RH finetune 1000 steps (`wm_ckpt_r60_rh_20260816`); AC 300 iters; re-gate `v4_gate_r60_20260816_wm_rh`: ① FAIL, ④ PASS; merge FAIL; yaml 未翻.
- **2026-08-18** — **① 结构性不可达入账**：In 表 goal-blind + 单 mock goal；M5d 变差是预测方向。待签字改 In 表（不降 ①、不加长训）。提案 `V4_SIGNAL1_STRUCTURAL_REFREEZE_PROPOSAL.md`。
- **2026-08-18（RH 校准曲线 DONE → 非 1:1，签字材料已出）** — 改了什么：125 离线跑 `v4_rh_progress_calib.py`，用已落盘 c2train a23/a4 JSON 的逐步 `progress` vs `Δ‖g‖`（≡ `analytic_progress`）。为什么：复核第 4 条的 4.2× 还只是总和，曲线才是逐步签字材料。依据：π **6.66×**、max-forward **4.18×**（yaw≡0）、retreat **wrong sign**；t0–t4 近校准、t≈6 起爆到 6–9 m/步。判定 **`sign_reopen_rh_progress_head`**。a4≡a23 重叠臂。未近 1:1 ⇒ **不**回头查 WM/z0。门禁未动；**改 RH 代码未动**。JSON `artifacts/v4_rh_progress_calib_c2train_20260818.json`。
- **2026-08-18（cos diag 结果复核 → RH 案重开；`clip_insufficient` 降级为 `unclassified`；`advance_goal_rel_body` code lead）** — 改了什么：**只加注记、事前表一字未改** —— 本文 §1 洞 3 加「选择偏差论据撤回」更正块、新增「cos diag 复核」七条块、「下一件」加收窄更新（改为 **RH 校准曲线**）、「处置」里「不开 RH 案」划删除线并注明推翻；`V4_SIGNAL1_SA_DIAG_STATUS` 标注「多出来的 λG0 来自侧向/垂向自由度」那句 + 追加复核节；`V4_C2_COS_DIAG_125_STATUS` 补 n=5 归因翻转 / Pearson 无功效 / up 分量 / 未报字段；提案 §4.1 注明本轮观测**三行皆不命中**；`LIVING_DOCS` §D/§E、`PROJECT_STATUS` §2 同步。**代码一行未动。** 为什么：cos diag 已 DONE（判定「不签 §4 In 表」**不变**），但复核发现四处记述错误、一处结论应被推翻，且事前判据标签自相矛盾 —— 洞仍在提出方（我），照 A.2/A.3/A.4 同例**标注不改写**。依据（全部本机读表 + 读码，无渲染无训练）：**(1) 标签**：`clip_insufficient` 是合取「① FAIL **且** cos<0」，cos = **+0.806/+0.762** ⇒ 第二项假 ⇒ 该行不成立，本轮应记 **`unclassified`**（「不签 §4」来自 cos-diag 那张后续事前表，不是本行）。**(2) 最强的数不是 first-act cos，是 `cos_path_goal` +0.075/+0.098**（≈85°；负 ep 3/7 与 2/8），但仍不签，理由须换成**洞 4**（训练侧单 `_mock_goal_episode()` + 评测侧 |az|≤0.8° ⇒ goal 是常量输入、可被 bias 吸收，In 表表达力增量 0）。**(3) Pearson 无功效**：+0.588(n=7, p≈0.17) / +0.217(n=8, p≈0.61)，两跑差 2.7×；`mean_real_minus_imagined≈−91` 混 horizon（想象 15 步 vs 真实 ≤200 步），须同窗口重算。**(4) RH 幅度校准失准 4.2×，且倒挂在想象内部即可复现**：(b) 最大前飞 yaw 恒 0 ⇒ 记账精确、15 步×上限 1.0 m = **恰好 15.0 m** 闭合（`‖goal_rel‖` 30→15.0 印证），RH `Σprogress` = **+62.60**（`w_progress=1.0` 米制，Σmaneuver −0.15=15×1.0×0.01 反证权重已施加）；π 闭合 12.2 m 得 **+81.64**（6.7×）⇒ **几何排序**（15.0 > 12.2）与 **RH 排序**（81.64 > 62.60）**相反**。**(5) 由此推翻**「多出来的 λG0 来自侧向/垂向自由度、不要读成再开 RH 案」—— 目标正前方时前飞即闭合最优，侧向/垂向不可能多闭合一米 ⇒ 盈余只能来自 RH 校准；当初两条依据的射程写宽了（A.3 作废只证臂不可实现；A.4 `fwdmax_ge_pi` 只证**方向**偏好没错），**都没检验幅度校准** ⇒ **RH 案重开**，依据比作废的 A.3 硬（(b) 完全可实现）。**(6) code lead（未修）**：`advance_goal_rel_body`（`goal_features.py:60-73`）只做 `g[:3] -= a[:3]`、**不按 `a[3]` 旋转** body goal，而 `imagine()` 逐步用它传播 RH conditioning（`imagination.py:164-165`）⇒ yaw≠0 的臂 conditioning 失真（π 的 12.2 m 亦不可靠；(b) yaw≡0 不受影响）—— 「倍率随臂变」的候选机制，改它会动 §A 全部数字 ⇒ **走签字**。**(7) 小项**：`goal_rel0` up 分量 0.07–1.04 待一句确认；PROMPT 里加的 `cos_mean10_act_xy_goal_body_xy`/`mean10_action` 未报（JSON 里有，零成本可补）。⇒ **下一件收窄**：不必先查 WM 转移 / z0 域差（更贵），改先在 125 上用**已落盘**的 `artifacts/v4_imagine_return_decomp_c2train_a23/a4_20260818.json` 逐步 `progress` vs `analytic_progress`（`goal_features.py:86`）作 **RH 校准曲线** —— 零渲染零训练零改码；仅当曲线接近 1:1 才回到 WM/z0 那条路。门禁未动：`δ_p=0.10`、`n≥8`（**不**降）、§4.1 阈值、shield/τ/depth 路径、`enable_policy_update=false`、yaml 全未改；**不签** §4 In 表这一判定不变。
- **2026-08-18（C2 ①-eval cos diag → 不签 In 表；洞 4 并入）** — 改了什么：125 跑 `v4_progress_diag.py` seed=0/1（C2 ckpt，`--imagine-horizon 15`）；补 A.3 欠账 ‖a‖ + Pearson；同日读码洞 4（评测 `goal_rel0` 构造性前向、gate partial 不落 cos、`rejections` vs 评测丢局）并入 §1。为什么：`clip_insufficient` 第二合取项须实测。依据：mean cos actor **+0.806 / +0.762**（同号 ≥0）；imagΣG **~85** vs real **~−5** ⇒ 事前表 **不签** §4 In 表。JSON `artifacts/v4_progress_diag_c2_seed{0,1}_20260818.json`。门禁未动。
- **2026-08-18（C2 从零重训 + ① 再 gate → `clip_insufficient`）** — 改了什么：125/H100 复跑 torch 单测 **24 passed**；`act_latent` 加 float64 盒夹（`6055cb1`）；`--fresh-c2` 重跑 §A（`n_action_clipped=0`）；H100 `train_v4_ac` 300 iter **未** warm-start → `v4_ac_ckpt_20260818_c2_fromscratch`；训后 §A `n_clip=0`、π a0 x=**+0.567**、λG0 59.09 > 最大前飞 47.78；125 ① 两跑均 FAIL（−7.43 / −3.53，**n=5** 非全权），④ PASS 0.000 vs 0.400。为什么：§4.1 顺序「单测 → §A n_clip=0 → 从零重训 → ①」。依据：提案事前表 `clip_helped` 要 λG0(π)≤最大前飞 **且** ① 差额缩小——前件不成立；① 仍 FAIL ⇒ **`clip_insufficient`**。`n≥8` 因 spawn-in-collision 未落地。门禁未动：`δ_p` / yaml / `enable_policy_update=false`。**同日复核补记（三个洞在提出方，事前表不改写，详见 §1）**：(1) `clip_insufficient` 行还有第二个合取项「首动作 cos <0」——训后 §A a0 x=+0.567 已转**前向**，而 ①-eval 逐 ep cos **未报**，goal-blind ⇒ probe goal 的正 cos 不可外推 ⇒ 该行**尚未验完**；(2) `clip_helped` 行的「λG0(π)≤最大前飞」当初写错，四自由度盒里纯前飞非最优，59.09>47.78 属预期（`goal_rel` 30→17.8 = 闭合 12.2 m ≈ 盒内理论上限 15 m 的 81%），**不**重开 RH 案；(3) n=5 是 spawn 的**非随机**丢失（偏向开阔起点），FAIL 方向对 n 稳健（补 3 局须各 ≈37.9 / 33.7，heuristic 每局仅 ~8.7）但两跑均不得入账为干净 gate，**④ PASS 同为非全权**（off-rate 2/5，on-rate=0 ⇒ ④b 大概率仍 `before_vacuous`）。⇒ 下一件 = **先取 ①-eval 逐 ep 首动作 cos + `goal_rel0` 方向分布**（判别 goal-blind 固定偏置 vs 想象-真实倒挂；后者 In 表修订不修），再决定签 §4；全权 ① 须先解 spawn，**不**降 `n`。
- **2026-08-18（§A DONE）** — 只读三臂 imagine：`(b)>(c)` λG0 47 vs 16，progress 主导，p_coll≈0。§4 充分。STATUS `V4_SIGNAL1_SA_DIAG_STATUS.md`。yaml 未翻。**←「§4 充分」已由下一条撤回；本行仅存历史。**
- **2026-08-18（C2 签字 → 有界策略分布已落地；本周期第一次动 V4 代码）** — 改了什么：**`actor_critic.py`** —— `ActorCriticConfig` 新增 `policy_class`（默认 `tanh_bounded_v1`）/`step_hz`（默认 `DEFAULT_STEP_HZ`）/`action_limits`（`None` ⇒ `body_delta_limits(1/step_hz)`，`__post_init__` 校验正值与维数），`action_scale` 默认 **3.0 → 1.0** 且语义降级为 **pre-tanh 增益**（原来是裸输出增益＝无界幅度乘子），`_action_dist` 改名 `_pre_dist`（返回**压缩前**高斯），`act_latent` 输出 `limits ⊙ tanh(u)`，`evaluate_actions` 用 `y=clamp(a/limits,±(1−1e-6))`、`u=atanh(y)`、`logp = logN(u) − Σlog(limits(1−y²))`、`ent = H(u) + Σlog(limits(1−y²))`（单样本估计；雅可比项对 actor 无梯度 ⇒ `entropy_scale=3e-4` 仍是 `Σlog_std` 的系数，未动），新增 `bounded`/`action_limits` 属性；**`update()` 对 legacy 类直接 `RuntimeError`**（红线：失效策略类只可 replay 不可 warm-start），`load_from_checkpoint` 对**缺** `policy_class` 的旧 payload 自动判为 `unbounded_gaussian_legacy` 并 warn（两类张量形状相同，这个自动判定是旧 ckpt 与「静默错误 warm-start」之间唯一的闸）。**`imagination.py`** —— 新增 keyword-only `action_limits`（默认 `None` ⇒ 历史行为逐位复现，保 §A/§A.3/§A.4 可回放）+ `ImaginedRollout.n_action_clipped` 计数器（C2 下必须 **0**，用**测**而不是**假设**来支撑「clip 是 no-op」）。**`corrector.py`** —— `_update_policy` 把 `ac.action_limits` 传进 `imagine()`，`n_action_clipped>0` 时 warn。**`planner.py`** —— `ImaginationPlanner.action_limits` 可选（**默认 `None` = 不动 V1 08-15 已 merge 行为**；打开会改 V1 部署路径 ⇒ 须 V1 re-gate）。**`train_v4_ac.py`** —— 训练前拒绝 legacy 类、日志打印 `policy_class/action_limits/step_hz/action_scale`、meta JSON 增这 4 个键（ckpt 内 `config.__dict__` 自带 `policy_class`+`action_limits`）。**`train_rl.py`** —— `_build_actor_critic` 从 **`env.step_hz`**（与 `collector` 夹的同一个值）派生动作盒并日志化；**`configs/aerial_rl.yaml`** —— `v4.action_scale: 1.0` + 注明「无 yaml 开关可关掉有界性」。**新增 `tests/test_action_space_consistency.py`（18 例）** —— 盒来自部署 `step_hz`（=[1.0,0.4,0.4,π/10]）、采样恒在盒内（含 `action_scale=50` 饱和情形）、logp 与 **torch 自带** `TransformedDistribution(Normal, [Tanh, Affine])` 对齐（独立实现交叉验）、压缩使熵下降、盒外动作 logp 有限、legacy 拒训、pre-C2 ckpt 载入即 legacy 且不可训、C2 ckpt round-trip、`imagine()` clip 在 C2 下 `n_action_clipped==0`、对越界策略计数正确、不传 limits 仍无界（审计回放）、planner 默认仍未夹 + 显式传 limits 才夹。为什么：签字项 = **C2 有界策略分布**；这不是调参而是补一条既有红线（「训练路径与部署一致」）在**动作轴**上的违反 —— §A.4 已确诊倒挂来自无界动作通道，而 REINFORCE 下只能改**采样律**、不能改**样本**。依据（本机可复算，torch 不在 Mac 上 ⇒ 用纯 numpy 独立验）：C2 密度在盒上积分 **1.000000 / 1.000000 / 1.000016 / 1.000000**（四组 `(mean,std,lim)`，梯形积分 4e6 点）⇒ 雅可比位置与符号正确；熵恒等式 MC `H(a)=−2.129664` vs `H(u)+E[log|detJ|]=−2.128682`（差 9.8e-4）；裁定所依赖的数复算一致：σ=0.6065、逐维被夹 **9.92%/50.96%/50.96%/60.45%**、四维全内 **0.085688**（8.57%）、代入 ckpt mean 为 **3.397e-6**（3.4e-6）。全量 `pytest experiments/aerial/rl/tests/` = **227 passed / 6 skipped**（6 skip = torch 门控，含新文件与 `test_actor_critic.py`，须在 H100/125 复跑）；`_v4_gate --self-check` **PASS**（门禁度量未受影响）。门禁未动：`δ_p=0.10`、`n≥8`、`enable_policy_update=false`、shield / τ / depth 路径、§4.1 阈值全未改；`w_progress`/`w_maneuver`/`success_bonus` 一个没碰（无界通道是**结构性**关掉的，不是靠加惩罚）；`goal` 张量未进 RSSM（V3 边界未越）。**未做**：H100 重训、§A 重跑、① 再 gate —— 下一件。
- **2026-08-18（一致性修设计复核 → 字面 clip 不够，裁定改三选一 C1/C2/C3）** — 改了什么：提案新增 **§4.1「一致性修怎么落」**（读码事实 + C1/C2/C3 对照表 + 事前判据 `clip_helped`/`clip_insufficient`/`spurious_pass` + planner 另记），§0 正交表「动作空间一致性」行补 REINFORCE 事实，§4 一致性修段指向 §4.1，§5 裁定项与签字栏改为 **C1/C2（推荐）/C3/不落** 四选，§A.4 事前判据表**原文不改写**、其下加「处置措辞不足」注（判定 `fwdmax_ge_pi` 与「不是 RH 案」**均不变**）；`V4_SIGNAL1_SA_DIAG_STATUS` 加末节同结论 + status 行；本文 §1「下一件」改为三选一并写清机理；`LIVING_DOCS` §D/2b、`PROJECT_STATUS` §2/§6 同步。为什么：A.4 处置只指定了 clip 的**位置**，没检查它落进什么**估计量** —— 又是提案方（我）事前判据里的洞，与 A.2/A.3 同类，故同样标注而不改写。依据（逐行核码 + 可复算的数）：`imagination.py` 纯 numpy 无梯度（docstring + `:116-137`）；actor 更新是 **REINFORCE** —— `corrector.py:176` `imagine(...)` → `actor_critic.update(rollout)`，`update()` 把 **`rollout.actions`** 交给 `evaluate_actions(z,a)` 得 `logp = Normal(mean,std).log_prob(a)`，再 `-(logp*adv).mean()`（`actor_critic.py:214-226`、`:257`、`:271`），**无梯度穿 `dynamics.step`**。故 C1 = 用未夹高斯给夹后动作算 logp ⇒ **(i)** 夹后动作在盒面上，真实采样律 = 盒内截断 + 盒面点质量的混合，`Normal.log_prob` 不是其密度 ⇒ 无偏性前提破掉（不是「近似」，是错似然）；**(ii)** `_log_std` 初值 −0.5（`actor_critic.py:144-146`）⇒ σ=**0.6065** 且**四维同值**，上限 [1.0,0.4,0.4,0.3142] ⇒ σ/上限 = 0.61/1.52/1.52/1.93，逐维被夹概率（mean=0）= 9.9%/51.0%/51.0%/60.4%，四维全在盒内 = **8.57%**；代入现 ckpt mean `a0=[-3.13,-1.23,-0.18,-0.05]` 为 **3.4e-6** ⇒ 每个样本都是盒面原子、同一 z 下只剩几个角点、**状态内动作方差≈0 ⇒ 学不出方向偏好**。推荐 **C2**：`u~N(mean,std)`、`a = limits ⊙ tanh(u)`、logp 加 tanh 雅可比 ⇒ 采样律本身在盒内、REINFORCE 无偏、σ 自动相对各维上限归一、无界通道**结构性关闭**（不靠调 `w_maneuver`、不靠加惩罚 ⇒ 不属「为凑过调参数」）；代价是改策略类 ⇒ **必须从零重训**（红线：禁 warm-start 现 ckpt）、`action_scale=3.0` 语义被 limits 取代需显式处理、ckpt schema/单测/`_v4_gate --self-check` 要更新。**另记（需核，不重开）**：`planner.default_candidates`（`planner.py:31-42`）也在未夹空间打分（候选含 `[max(|dx|,1.0),0,0,0]`），到 `collector.py:167` 才夹 ⇒ V1 部署侧同源不一致（打分空间 ≠ 执行空间）；V1-④ 已 PASS，**不**重开 08-15 merge，但落 C2 时应同时覆盖候选集。门禁未动：`δ_p=0.10`、`n=8`、`enable_policy_update=false`、yaml / shield / τ / depth 路径全未改；**代码一行未动**（`imagination.py` / `actor_critic.py` / `planner.py` 均未改），等签字。
- **2026-08-18（§A.4 DONE → `fwdmax_ge_pi`；A.3traj 印证欠匹配）** — **←本条「先落动作空间一致性」的具体写法已由上一条改为 C1/C2/C3 三选一；A.4 判定与数字不变。** 改了什么：本文 §1 把「下一件 = §A.4」改为 **「A.4 = `fwdmax_ge_pi`、下一件 = 动作空间一致性裁定」**；`V4_SIGNAL1_SA_DIAG_STATUS` 追加 A.4 实测表 + A.3traj 配套表（事前判据表**不改写**）；提案 §A.4 填判定、§5 勾 A.4 项，签字栏 A.4 填 `fwdmax_ge_pi`，「动作空间一致性裁定」仍空（待人签）；`LIVING_DOCS` 2b/2c + §D/§E、`PROJECT_STATUS` §2 + §6 同步。为什么：A.4 预提交第一支触发 —— 夹后最大前飞赢过夹后 π，倒挂来自无界动作通道。依据：125 seed=0 `--clip-actions` JSON `artifacts/v4_imagine_return_decomp_a4_20260818.json` —— λG0 (b) **47.64** ≥ (a) **18.25**（差额 29.4）；π 夹后 a0=`[-1.0,-0.309,-0.27,-0.163]` 仍后向但 ‖a0[:3]‖=1.12 落在 limits 内，goal 30→45.9（`arrived=false`）；(b)>(c) 47.64 vs 15.87 与无 seed 口径 49.65/15.99 同号。配套未夹 `--match-basis traj` JSON `..._a3traj_20260818.json`：`match_scale=15.59`（相对旧 A.3 a0=3.59 为 **4.3×**，相对本跑 a0=3.13 为 **5.0×**），印证作废理由 1； (b3)@15.59 λG0 **18.21**、过冲 30→**203.9**；(c3) Σp_coll **−60.14**（仅 OOD 15 m/步才亮，可实现 1 m/步仍死）。`--match-scale auto` 不是 float，命令用 `0`（脚本 auto 约定）；`~/data`/`~/ckpt` 不存在，改用 artifact 路径。门禁未动：`δ_p=0.10`、`n=8`、`enable_policy_update=false`、yaml / `imagine()` 本体全未改（clip 只包策略）。
- **2026-08-18（§A.3 复核 → 判定作废，追加 §A.4）** — **←本条「下一件 = §A.4」已由上一条完成；判定作废三条理由与数字仍成立，保留存审计链。** 改了什么：本文 §1 把「下一件 = 先修 RH」改为 **「A.3 判定作废、下一件 = §A.4 只读」**；`V4_SIGNAL1_SA_DIAG_STATUS` 加「A.3 判定作废」段 + Next（原实测表与 `b3_le_a` 字面判定**保留不改写**，处置行划删除线并注明撤回）；提案加 **§A.3 复核** + **§A.4 判据/命令**，A.3 事前判据表标「已被 §A.4 取代」但**原文保留**，§0 正交表新增「动作空间一致性」一轴，§4 新增「独立于本提案、可先落的一致性修」，§5/签字栏加 A.4 与动作空间裁定两项；`v4_imagine_return_decomp.py` 追加 `--clip-actions`（`_ClippedPolicy` 用部署同一 `clip_body_delta`+`body_delta_limits(1/step_hz)` 包**策略**，`imagine()` 本体不动 → 仍只读）、`--step-hz`、`--seed`、`--match-basis {a0,traj}`（默认 `a0` 以复现 A.3）、`_apply_a4` 预提交判据、JSON 增 `A4`/`seed`/`clip_actions`/`body_delta_limits`；`LIVING_DOCS` A 表 2b/2c + §D、`PROJECT_STATUS` §2 + §6 同步。为什么：`b3_le_a` 字面成立但**臂无效**，故其处置「先修 RH」无依据 —— 洞又出在提案方（我）写的事前判据上，与 A.2 那个洞同类，故照同一办法标 superseded 而**不**事后改写。依据（三条，均逐字核过代码）：**(1) 幅度欠匹配 ~4.6×** —— `match_scale` 取 `act0_norm3_mean`=3.591 只匹配第 0 步，π 轨迹均值 ‖a[:3]‖**≈16.5**，由 Σmaneuver 2.47÷`w_maneuver`0.01=Σ‖a‖247÷15 与 ‖goal_rel‖ 30→253.9 需 ≈14.9/步 **两条独立算路互证**；脚本已算 `act_norm3_mean`（`:331`）但 auto 走 `act0_`（`:366`）。**(2) 五臂全在物理不可实现区** —— `env.step_hz=5.0`（`configs/aerial_rl.yaml:20`）⇒ 部署每步夹 `body_delta_limits(0.2)`=[1.0,0.4,0.4,0.314]（`env/action.py:57`，`collector.py:167`/`airsim_env.py:170`），而 `imagine()` 一次不夹（`imagination.py:120-127`）；于是 **(b) `[+1,0,0,0]` 恰等于可实现最大前飞步，λG0 49.65 vs 可实现最大后退 15.99（3.1×）⇒ 可实现集合内 RH 方向偏好正确**，π 的 103.63 只来自跑出集合 3.6×（a0）→16×（轨迹），而 `w_progress:w_maneuver=1.0:0.01`=**100:1** 使「顶大 ‖a‖ 净赚」恒真、无需方向判断；(b3)@3.591 现实夹回**就是 (b)**（已用真 `clip_body_delta` 验证）。同时更正上一条记录里的错误：**`action_scale=3` 不是饱和界**，`_MLP` 末层裸 `nn.Linear` + `mean=actor(z)*action_scale`（`actor_critic.py:200`）⇒ 幅度**无上界**，`v4:` 块也未暴露、`from_config` 不读；另 RH 训练位移 = `body_vel×reward_dt=0.2` ≤ ~1 m/步（`configs/aerial_rl.yaml:74-76`）⇒ 想象查 3.6–16 m 属**幅度轴 OOD**，与 ‖goal_rel‖ 8.5× 是两条独立 OOD。**(3) (b3) 另被扣两刀** —— 15×3.591=53.87>30 ⇒ 第 ~8.4 步冲过目标，末态 |30−53.87|=**23.87**（实测 23.9 ✓，(c3) 30+53.87=83.87 vs 83.9 ✓，两个精确对上反验 `_goal_dist_traj`），故 STATUS「23.9（靠近）」是读反；`_apply_a3` 只对 (a) 查 arrival（`:134`）。**另记两项**：`p_coll≈6e-4` 横跨所有臂（含头对头 1 m/步 15 步撞墙）⇒ 想象里碰撞头**近乎是死的**，④ 想象刹车同样无信号（另案，不是「碰撞项已排除」的好消息）；A.2 首跑（`14d0f06`）与 A.3 同跑（`2afcb33`）**同 ckpt 同 z0 结果不同**（λG0 π 104.72→103.63、前飞 47.02→**49.65**(+5.6%)、‖a0‖ 3.36→3.59）—— 脚本当时**无 seed**，故 33×/35× 不得报两位有效数字；判定符号安全（A.3 差额 43.8 ≫ 6%）。门禁未动：`δ_p=0.10`、`n=8`、`enable_policy_update=false`、yaml 全未改；`--clip-actions` 只包策略，`imagine()` 行为不变（是否在 `imagine()` 里永久落 clip 已列入签字栏，A.4 后裁定）。
- **2026-08-18（§A.3 DONE → `b3_le_a`）** — **←本条处置「先修 RH」已由上一条撤回；判定与数字仍为实测，保留存审计链。** 改了什么：只读幅度匹配臂 (b3)/(c3) @ π ‖a0[:3]‖=**3.59**；活文档把「§4 充分」从「待 A.3」改为 **不成立、先修 RH**。为什么：A.3 预提交判据 (b3)≤(a) ⇒ RH 在 π 的幅度下仍偏好其后向向量。依据：λG0 π **103.63** > 前飞@3.59 **59.85** > 后退@3.59 **15.31**；Σprogress 142 vs 83 vs 9；`pi_imagined_arrival=false`（‖goal_rel‖ 30→254，排除「想象以为到达」）。yaml / `δ_p` / In 表均未改。
- **2026-08-18（§A 复核 → 撤回「§4 充分」，追加 A.3）** — 改了什么：`V4_SIGNAL1_SA_DIAG_STATUS`「§4 可直接执行」**撤回**并加更正段；提案新增 **§A.3 幅度匹配臂**判据 + 命令，§5 首项改为「碰撞项通道已排除」并新增 A.3 待填项；`v4_imagine_return_decomp.py` 追加 (b3)/(c3) 匹配幅度臂、`--match-scale`、五臂逐步 `progress_t`/`p_coll_t`/`‖a_t‖`/`‖goal_rel‖_t` 与 `_apply_a3`；本文 §1、`LIVING_DOCS` A 表 + §D、`PROJECT_STATUS` §2 + 洞表同步。为什么：A.2 三行判据只比 (b)/(c) 两个**单位幅度**常量，从未与 π 自比，**结构上探测不到幅度通道**（判据的洞在提出方，即我）。依据：同表第一行 —— π 想象 λG0 **+104.72** 为最高，真实 ① **−8.74** 为最低（前飞 +47.02 / heur +8.42），**排序倒挂**＝命门 A 的直接证据；`a0` 模长 ≈3.36（`action_scale=3` 饱和），幅度 3.4× 换 Σprogress 2.3×，而 Σmaneuver 仅 −0.15→−2.47，即 progress +81.92 对惩罚 +2.32（≈**35× 欠罚**）。同时更正残差段：排除 `p_coll` 配平案成立，但「多样 goal 重训可消化」**无依据**（多样 goal 修方向条件化，不改幅度欠罚）。另记三项未报：`goal_rel` 被 `g−=a[:3]` 推至 ≈80（训练 ~30）故 (a)/(c) progress 属 OOD 查询；(a) 逐步 `‖goal_rel‖` 未报（若收到 ~0 则病灶在 z 转移保真，另案）；`goal_rel0` 只用 ep0 一个值套 8 个 z0，不得读成 goal 分布覆盖。未改代码行为以外的任何门禁：`δ_p=0.10`、`n=8`、yaml 全未动；A.3 为**只读**。
- **2026-08-18（提案审核 4 处修订）** — 复核提案属实（规格 In 表、`_MLP(ld,·)`、`imagine(goal_rel0=)`、`LatentActorDeployPolicy.act`、`loop.episodes` 长度 1、cos −0.88 出处均逐字对上）后改 4 处：**(1)**「数学上不可达」→「**不可稳健达成**」（依据：实测 `goal_body0≈[+30,0,0.85]` 偏前，goal-blind 常量前飞能得正 progress，故非数学不可能；同时把「固定前向偏置冒充定向」列入明确不做，堵凑过通道）；**(2)** 新增 **§A 签字前置只读诊断**为第一件事，理由：根因只解释首动作**恒定**、不解释方向为**负**（训练/部署 goal 同向偏前），已静态排除 `advance_goal_rel_body` 符号（`goal_features.py:71` 为 `g-disp`，正确）与 `maneuver=‖a‖`（与方向无关），余下嫌疑为 RH `progress` 与 `p_coll`；A.2 判据先写死；**(3)** §4 补 ① 再 gate `n≥8`（M5d ① 实跑 n=7，`v4_metrics.py:46` 下非全权）与「训练 goal 同分布、不同实例」；**(4)** §5 增 **V3 范围裁定**签字行（改 actor/critic MLP 输入是否触及「不给 RSSM 加 goal 张量」；提案方读法为不触及）。§2 第三行补 artifact/log/script 出处。未改代码 / `δ_p` / yaml。
- **2026-08-17(goal+z0)** — M5d done: goal inject + headon RGB z0 AC 300 iters (`v4_ac_ckpt_20260817_wm_rh_goal_rgb`); re-gate `v4_gate_r60_20260817_wm_rh_goal`: ① FAIL (−8.74), ④ PASS; merge FAIL; yaml 未翻.

---

## 4. Latest gate numbers (goal+RGB z0, 2026-08-17)

| Signal | Criterion | Result | Numbers |
|---|---|---|---|
| **V4-①** | actor ≥ heur × 1.10 | ❌ | actor_mean **−8.74** vs heur **8.42** (target **9.26**); n=7 |
| **V4-④** | v4_hard ≤ v1_hard | ✅ | v4_hard **0.000** vs v1 **0.429** (remeasured same starts) |
| **Merge** | both pass | ❌ | `ok=false` `{1: false, 4: true}` |

Artifacts: `experiments/aerial/rl/artifacts/v4_gate_r60_20260817_wm_rh_goal/v4_gate_r60_20260816.json`

### Prior (reward-head WM, 2026-08-16 — superseded)

| Signal | Criterion | Result | Numbers |
|---|---|---|---|
| **V4-①** | actor ≥ heur × 1.10 | ❌ | actor_mean **−3.17** vs heur **7.44** (target **8.18**); n=5 |
| **V4-④** | v4_hard ≤ v1_hard | ✅ | v4_hard **0.143** vs v1 **0.25** (remeasured same starts) |
| **Merge** | both pass | ❌ | `ok=false` `{1: false, 4: true}` |

Artifacts: `experiments/aerial/rl/artifacts/v4_gate_r60_20260816_wm_rh/v4_gate_r60_20260816.json`

### Prior (encode-train, legacy RH skipped — superseded)
| **V4-①** | ❌ | actor_mean **−68.88** vs heur **10.66** |
| **V4-④** | ❌ | v4_hard **0.143** vs v1 **0.00** |

Artifacts: `experiments/aerial/rl/artifacts/v4_gate_r60_20260816_wm/v4_gate_r60_20260816.json`
