# Mac → 125 主线交接（2026-08-27）

> **权威入口**：[`experiments/aerial/RUNBOOK_wam_imagination.md`](../../experiments/aerial/RUNBOOK_wam_imagination.md)  
> **STATUS**：[`V4_RUNBOOK_125_STATUS.md`](V4_RUNBOOK_125_STATUS.md)  
> **Mac**：文档同步；长跑/评测/文档推进默认由 **cursor-125 Agent** 执行。勿开罩子大战；偏离主线立刻改回。

---

## 你是谁 / 主线是什么

- 产品主线：**单目 RGB（+IMU）→ 世界模型想象选动作 → 到点**；深度/限速罩只做安全。  
- 学得策略才是大脑；启发式 / 路点跟踪 **不是** 部署大脑。  
- 目标当前是**坐标特权**，不是视觉搜目标。  
- 闸门：`A → B 过阈 → E`；`C → D → B 不回退 → E`；`G PASS → F`。

---

## 已完成（勿重做）

| 项 | 事实 |
|----|------|
| **A 结构** | `condition_on_goal`；想象环传 `goal_rel`；125 `test_actor_goal_cond` 2 passed |
| **B 诊断** | 基线不够：`artifacts/wam_imagine_coll_rank_p45_n32_20260827.json`（p_coll≈常数 0） |
| **B 已开修** | `coll_near_depth_m=5` + auto pos_weight；`p(coll\|z,a)`；前向才打近深 soft |
| **125 短 FT** | `wm_ckpt_coll_ft_act_20260827/wm_step_400.pt` → 复测仍 insufficient：`artifacts/wam_imagine_coll_rank_collft_act_n32_20260827.json`（前/侧均≈0.25，gap≈0） |
| **H100 全量 WM** | `wm_ckpt_coll_full_20260827/wm_step_1000.pt`（init step500 +500）；125 复测 `artifacts/wam_imagine_coll_rank_h100full_20260827.json`：**median_p_coll_gap=0.0018** → 仍 insufficient |
| **C2 采集** | `dataset_wam_loop_20260827/` — **31 ep**（30 OK / 1 quarantine）；`dataset_meta.json` 已写 |
| **禁令** | **未过 B 阈禁止开 E 猛训 π**；禁止关罩 / 放大到点半径凑过 |

---

## 当前阻塞与下一发（按优先级）

### P0 — 碰撞头真正可用（阻塞 E）

H100 全量 WM（2026-08-27）**已跑完仍不够**：`median_p_coll_gap=0.0018`（阈 0.05）。头不再塌零（mean_p_coll≈0.23）但前/侧几乎无差。

仍须（按优先级）：

0. **先跑 B′ 诊断**（见 runbook §B′）：latent 探针 → encode 对照 → 再决定 coll v2 短训或改损失。  
1. 加强 coll 监督 / 损失形，或换更「近障+到点」导航向语料再训 D。  
2. 每次训后回 **125** 重跑：

```bash
source experiments/aerial/scripts/env_4090.sh
$AERIAL_PY experiments/aerial/scripts/wam_imagine_coll_rank.py \
  --dataset experiments/aerial/rl/artifacts/dataset_v0_p45_merged_20260821 \
  --wm-ckpt <新_wm_ckpt> \
  --max-samples 32 --max-center-depth-m 12 --stride 2 \
  --out artifacts/wam_imagine_coll_rank_<日期>.json
```

3. 过阈条件：`verdict.useful==true` 且 **`median_p_coll_gap ≥ 0.05`**（仅回报差不算）。

相关代码：`experiments/aerial/rl/dynamics_torch.py`（`_coll_logits` / `_collision_train_targets` / `update_coll_head`）、`scripts/wam_coll_head_ft.py`、`scripts/wam_imagine_coll_rank.py`、`configs/aerial_rl.yaml`（`coll_near_depth_m`）。

### P1 — C2 语料（已完成，可进 D 预热）

- **31 ep** @ 5 Hz + goal + depth：`dataset_wam_loop_20260827/`（meta `role=wm_loop`）。  
- 可用作 D 预热 WM；**B 过阈前仍禁止 E**。

### 明确不要做

- 新开深度 FT / 罩子战役当主线  
- B 未过就 `enable_policy_update` / 接大脑  
- 把坐标目标结果写成「视觉找到目标」

---

## 机器分工

| 机 | 职责 |
|----|------|
| **125（本 agent）** | 评测、coll_rank、C2 采集、短 FT、文档 STATUS/runbook 勾选；**唯一跳板连 H100** |
| **H100** | 全量 WM / 日后策略长训 — **只从 125 SSH 上去训**，Mac 不直连 |
| **Mac** | 文档/接线；**禁止**从 Mac 开 H100 长训 |

**常驻规则（2026-08-27）**：凡涉及 H100，一律由 **125 agent** 发起连接与作业；不要在 Mac 上 `ssh` H100。

---

## 粘贴给 125 Agent 的开工句（可直接用）

```text
按 experiments/aerial/RUNBOOK_wam_imagination.md 主线推进。
读 docs/handover/V4_WAM_125_HANDOFF_20260827.md。
当前：A 完成；C2 31 ep 完成；H100 全量 WM 已训 + B 复测仍不够（gap=0.0018）；禁止开 E。
优先：诊断/修碰撞头监督 → 125→H100 再训 WM → 125 重跑 wam_imagine_coll_rank.py 直到 median_p_coll_gap≥0.05。
一切围绕想象到点；偏离立刻修正。Mac 不直连 H100。
```
